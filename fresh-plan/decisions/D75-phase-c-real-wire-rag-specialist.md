# D75 — 2026-05-17 — Phase C real-wire RAG specialist (closes C7; activates D50 §B.1; resolves D50 §D D-5 composition contract — WRAP)

**Decision (substantive; seventh Phase C workstream lock per D68 §A C7; FIRES pre-lock probe per refined-skip rule — NEW contract content: D50 §B.1 SkillExecutionError forward-bar ACTIVATION + D50 §D D-5 composition contract RESOLUTION as first concrete precedent)**: Phase C workstream C7 (real-wire RAG specialist per D68 §A + §C closure items (c) + (f)) lands `RealWireRAGSpecialist` — a new specialist subclass that REPLACES the B7 `RAGSpecialist` stub by invoking the c3 `RealWireMCPClientAdapter` (D71) end-to-end through the bound `mcp-server-ext:mcp-real-wire-adapter` provision (the 0.2.0 adapter from D71). NEW extension at `extensions/rag-via-mcp-ext/0.2.0/` (preserves the 0.1.0 B7 stub extension untouched per D41 two-version parity precedent) registers a NEW specialist provision id `rag-realwire-specialist` (DISTINCT from the 0.1.0 `rag-specialist` stub) whose `required-adapter-bindings` references the c3 real-wire adapter provision rather than the B7 stub retriever adapter. Closes Phase C closure items (c) + (f) per D68 §C: (c) real-wire RAG-via-MCP end-to-end through in-process MCP server harness per scope-cut C12; (f) SkillExecutionError per starter category (`domain-error` / `external-dependency-error` / `skill-execution` / `unknown`) raised from real-wire `handle_skill` body. Resolves D50 §D D-5 (AdapterCallError-inside-handle_skill composition contract) by choosing **WRAP**: `AdapterCallError` caught from `adapter.call('retrieve', ...)` is wrapped as `SkillExecutionError(category="external-dependency-error", detail={...})` with the original AdapterCallError chained via Python's `__cause__` (`raise ... from exc`) — first concrete D-5 precedent per D50 §D D-5 framing ("first concrete real-wire impl establishes precedent").

## A. Scope of cluster

ONE specialist class with one skill (`retrieve`) and four-way SkillExecutionError category dispatch inside the single `handle_skill` body. Honest cluster cardinality: **1 path** (mirrors D50 + D52 + D64 + D66 + D69 + D70 + D71 + D72 + D74 1-path scope; not inflated to D73's 3-path because c7's four category branches collapse onto a single in-method failure-classification cascade — no second triad). The single triad path is the skill-body failure surface; per-category sub-paths are surface-detail variants within the same triad framing.

In scope:

- **New extension** at `fresh-plan/impl/extensions/rag-via-mcp-ext/0.2.0/` (3 files: `extension-manifest.json` + `specialist.json` + `retrieval-task.schema.json`) — mirrors the 0.1.0 B7 stub structure minus the obsolete `adapter.json` (the 0.2.0 specialist binds the c3 D71 real-wire adapter; no rag-extension-local adapter provision). Provisions `kind=specialist id=rag-realwire-specialist` with `required-adapter-bindings=["mcp-server-ext:mcp-real-wire-adapter"]` referencing the c3 adapter provision directly. The 0.1.0 stub at `rag-via-mcp-ext/0.1.0/` remains untouched per D41 two-version parity.
- **New runtime class** `RealWireRAGSpecialist` in `fresh-plan/impl/src/fresh_plan/runtime/specialist.py:391-650` (class body ~260 LOC including docstring + 4-category dispatch ladder + result-parsing). Subclass of `Specialist` (D19 base) overriding `handle_skill` only — inherits `attach_workspace` (D64 §B.1 emit-attribution closure-wrap), `register_skills`, `on_event`. File grew from ~423 LOC to 685 LOC (+262).
- **Registry entry** `_SPECIALIST_CLASSES["rag-realwire-specialist"] = RealWireRAGSpecialist` at `specialist.py:657` enabling `load_specialist_from_provision` dispatch. Existing entries (`generic-specialist` → GenericSpecialist; `rag-specialist` → RAGSpecialist) stand unchanged per D41 two-version parity.
- **10 tests** at `fresh-plan/impl/tests/test_rag_realwire_specialist.py` (NEW; 419 LOC; 10 `def test_` functions; no parametrize). Coverage: (1) structural — 0.2.0 provision resolves to RealWireRAGSpecialist; (2) closure item (c) happy-path retrieve end-to-end + real chunks threaded through; (3) closure item (c) + D64 §B.1 emit-attribution verification (emitting-specialist field present); (4) domain-error on empty query; (5) domain-error on non-string query; (6) domain-error on invalid k; (7) external-dependency-error WRAPs AdapterCallError with __cause__ chain (D50 §D D-5 RESOLUTION); (8) skill-execution on malformed adapter result; (9) unknown on unexpected exception from adapter; (10) NotImplementedError pre-condition guard on unknown skill id (D50 §D D-1 Python idiom).
- **Test fixture** at `fresh-plan/impl/tests/fixtures/workspace-rag-realwire-via-mcp/` — `workspace.json` + mirrored extensions tree (inprocess-substrate-ext/0.1.0 + generic-shape-ext/0.1.0 with the `retriever` role added + mcp-server-ext/0.2.0 + rag-via-mcp-ext/0.2.0); binds `primary-mcp-realwire` adapter (D71 c3 real-wire adapter) + `primary-rag-realwire-specialist` specialist.
- **In-process MCP server harness** in test module — `_build_retrieve_server` registers a `retrieve` tool returning canned TextContent blocks via the SDK's `mcp.server.lowlevel.Server` + `mcp.shared.memory.create_connected_server_and_client_session` memory bridge per scope-cut C12 (same pattern as D71 c3 echo-server harness).
- **No `pyproject.toml` change** — c7 reuses the `mcp>=1.0` dep added at D71 c3 + the c3 RealWireMCPClientAdapter unchanged. Zero footprint delta.

Out of scope (deferred per §D):

- Real-corpus retrieval (real embeddings + real vector DB + real document store) — c7 scope is the framework composition pattern (specialist → adapter → MCP server → result threading), NOT the retrieval-system implementation. Deferred to Phase D pioneer-instance OR future Cref refinement.
- Generic-specialist real-wire (B6 GenericSpecialist stub) — c7 focuses on RAG specialist; generic-specialist real-wire deferred to Phase D pioneer-instance per D68 §A out-of-scope framing.
- Streaming retrieval responses, batch retrieve, cross-corpus federation, per-result citation extraction — Phase D+ refinements when concrete pioneer-instance surfaces concrete need.
- Specialist-side caching / deduplication of retrieve results — c7 ships a stateless body; caching is shape-policy or specialist-internal-policy concern, not framework concern.
- Per-shape SkillExecutionError category extensions beyond the practitioner-shape-flavored starter set — per D50 §D D-2 deferred to extension-registered category vocabulary per D29 namespacing.

## B. Triad applied per path

### B.1 — handle_skill body failure under real-wire conditions (NEW shape per D75; ACTIVATES D50 §B.1 forward-bar + RESOLVES D50 §D D-5 composition contract)

| Triad element | Lock |
|---|---|
| **Detection** | The `RealWireRAGSpecialist.handle_skill('retrieve', params)` body classifies failure modes into four SkillExecutionError categories per D50 §B.1 starter vocabulary. Detection raise-points form a deterministic cascade: (1) **domain-error** — params validation BEFORE the action emit; query must be a non-empty string + k must be int + 1 ≤ k ≤ 100 (sanity bar). (2) **external-dependency-error** — `try: adapter.call(...) except AdapterCallError as exc: raise SkillExecutionError(category="external-dependency-error", detail={...}) from exc` per D50 §D D-5 RESOLUTION (WRAP). (3) **skill-execution** — result-parsing invariant violations AFTER adapter returns: content not a list, content block not a dict, content block missing `text` field. (4) **unknown** — bare `except Exception` catch-all after the AdapterCallError + SkillExecutionError re-raise branches (the catch-all preserves any pre-classified SkillExecutionError by isinstance-aware re-raise). Per D71 §B.1 pre-wire convention the action event is emitted BEFORE the adapter call so intent is recorded in the chain regardless of call outcome. Per D64 §B.1 emit-attribution: INHERITED via `Specialist.attach_workspace` closure-wrap (specialist.py:226-237) — RealWireRAGSpecialist overrides nothing on attach; the inherited wrapper auto-fills `emitting-specialist=<binding-id>` on the emitted action event. |
| **Surface** | `SkillExecutionError` carries structured fields per D50 §B.1: `specialist_id` (the specialist spec id), `skill_id` (the skill_id parameter), `category` (one of `domain-error` / `external-dependency-error` / `skill-execution` / `unknown`), `detail` (dict carrying domain-specific structured context). For the external-dependency-error path (D-5 RESOLUTION): `detail` contains `underlying` (str(AdapterCallError)) + `adapter-category` (original AdapterCallError.category) + `adapter-id` (the adapter's spec id) + `call-target` (the tool name passed to adapter.call). The original AdapterCallError is preserved via Python's `__cause__` chain (`raise ... from exc`) for caller introspection. For unknown: `detail` contains `reason` + `exception-type` + `exception` (str). Two invocation paths surface differently per D50 §B.1: (a) direct `handle_skill` calls — SkillExecutionError propagates raw; caller catches directly; (b) on_event-triggered (specialist's on_event delegates to its own handle_skill — N/A for c7 since RealWireRAGSpecialist's on_event is the inherited no-op default). |
| **Recovery** | Caller catches `SkillExecutionError` + introspects `category` for per-category recovery: `domain-error` → correct params + retry; `external-dependency-error` → introspect `__cause__` (the original AdapterCallError) for wire-layer detail (transport / auth / timeout / protocol-error / upstream-error / unknown per D71 §B.1) + per-category retry-or-abort policy; `skill-execution` → server-side data shape contract violation, per-server-impl debug; `unknown` → bug-report territory. Framework does NOT mandate retry semantics per D50 §B.1 explicit non-prescription. Per the WRAP precedent set by c7: future real-wire specialists MAY choose PROPAGATE-RAW instead per D50 §D D-5 explicit choice ("Both reasonable; caller catches either"); the specialist's docstring SHALL state which composition convention applies. |

## C. Impl follow-through (landed atomically with this D-entry per [design+impl] criterion)

Files modified:

- `fresh-plan/impl/src/fresh_plan/runtime/specialist.py` — add `RealWireRAGSpecialist` dataclass (lines 391-650; ~262 LOC including docstring + 4-category dispatch ladder + result-parsing block) + register in `_SPECIALIST_CLASSES` dict (line 657). File grew from 423 LOC to 685 LOC (+262 net). Existing `GenericSpecialist` (line 313) + `RAGSpecialist` (line 338) preserved unchanged per D41 two-version parity.

Files created:

- `fresh-plan/impl/extensions/rag-via-mcp-ext/0.2.0/extension-manifest.json` — provision-id `rag-realwire-specialist`; spec-ref `./specialist.json`; vocabulary-registration for `work-unit.kind` identifier `retrieval-task` (mirrored from 0.1.0). Dependency on `mcp-server-ext >=0.2.0` (the c3 real-wire adapter provision).
- `fresh-plan/impl/extensions/rag-via-mcp-ext/0.2.0/specialist.json` — `id=rag-realwire-specialist version=0.2.0`; `required-adapter-bindings=["mcp-server-ext:mcp-real-wire-adapter"]` (the c3 D71 real-wire adapter provision); same role + skill + capability-requirements shape as 0.1.0 stub.
- `fresh-plan/impl/extensions/rag-via-mcp-ext/0.2.0/retrieval-task.schema.json` — stub schema (preserves 0.1.0 work-unit-kind identifier resolution path; Phase D+ pioneer-instance refines).
- `fresh-plan/impl/tests/fixtures/workspace-rag-realwire-via-mcp/workspace.json` — manifest binding `mcp-server-ext:mcp-real-wire-adapter` 0.2.0 as `primary-mcp-realwire` adapter binding + `rag-via-mcp-ext:rag-realwire-specialist` 0.2.0 as `primary-rag-realwire-specialist` specialist binding.
- `fresh-plan/impl/tests/fixtures/workspace-rag-realwire-via-mcp/extensions/` — mirrored extensions tree (inprocess-substrate-ext/0.1.0 + generic-shape-ext/0.1.0 with the `retriever` role added + mcp-server-ext/0.2.0 + rag-via-mcp-ext/0.2.0).
- `fresh-plan/impl/tests/test_rag_realwire_specialist.py` — NEW test file; 419 LOC; 10 `def test_` functions (10 collected; no parametrize).

Tests: `.venv/bin/python -m pytest -q` from `fresh-plan/impl/` → **322 passed** (312 baseline + 10 new). pytest baseline preservation per C6 confirmed.

Estimated impl size: ~262 LOC net new specialist class (subclass body + docstring + 4-category dispatch + result-parsing) + 419 LOC test file + 10 collected tests + 3 new extension JSON files + fixture mirror. Atomic [design+impl] commit per CLIPPY-COMPANION §"Artifact classes" — same-git-commit mechanical test satisfied.

## D. What is NOT in this decision

The five explicit DEFERS surfaced by combining D50 §D D-1 through D-6 framing + c7-specific scope-cut concerns:

- **D-1 — Real-corpus retrieval (real embeddings + vector DB + document store)**: c7 scope is the framework composition pattern, NOT the retrieval-system implementation. The in-process MCP server's `retrieve` tool returns synthetic TextContent blocks; production retrieval requires real embedding model + vector DB + document store. Deferred to Phase D pioneer-instance OR future Cref refinement; the c7 specialist class is the wire-bound shell that real-corpus implementations plug into without further framework change (per D50 §D D-1 + D75 §B.1 result-parsing pattern stands across corpus impls).

- **D-2 — Per-shape SkillExecutionError category vocabulary extensions**: c7 commits the D50 §B.1 starter vocabulary (`domain-error` / `external-dependency-error` / `skill-execution` / `unknown`) — practitioner-shape-flavored per D50 §D D-2. Other shapes (Phase E multi-shape work; autonomous-business-shape, financial-trading-shape per D37) MAY register additional categories per D29 namespacing (`policy-violation`, `quorum-failure`, `market-rejection`, `citation-missing` per practitioner-shape, etc.). c7 does NOT pre-emptively register such categories; deferred per D50 §D D-2.

- **D-3 — Streaming retrieval / batch retrieve / cross-corpus federation**: c7 ships a single-call request/response shape: one query + one k → one chunks-list. Streaming retrieval (incremental result delivery via subscription per D44 + D58), batch retrieve (multiple queries in one call), cross-corpus federation (single retrieve spanning multiple bound MCP retrievers), per-result citation extraction (richer chunk shape with source-doc-id + offset + score) are all Phase D+ refinements when concrete pioneer-instance surfaces concrete need.

- **D-4 — Specialist-side caching / deduplication / result post-processing**: c7's `handle_skill` body is stateless — every call invokes `adapter.call('retrieve', ...)` afresh. Cache-aware specialist behavior (LRU on query-text; deduplication of overlapping chunks across calls; result post-processing such as re-ranking by relevance score) is shape-policy OR specialist-internal-policy concern, NOT framework-core concern. Phase D pioneer-instance may layer such behavior via subclassing OR via extension-registered configuration-driven policy hooks; c7 ships the wire-bound shell only.

- **D-5 — Generic-specialist real-wire equivalent**: c7 closes the RAG specialist real-wire path. The B6 GenericSpecialist stub at `specialist.py:313` remains a stub (no real-wire ACTIVATION). When concrete Phase D pioneer-instance surfaces concrete need for a generic-specialist-shaped real-wire impl (e.g., a tool-calling agent specialist routing to multiple bound adapters), a parallel pattern applies: NEW subclass + NEW extension-version + register in `_SPECIALIST_CLASSES`. c7 establishes the pattern; D-5 is the explicit cross-impl deferral.

Other items NOT in this decision:

- **No retroactive rewrite of D16, D19, D29, D44, D45, D46, D47, D48, D50, D57, D60, D64, D68, D69, D70, D71, D72, D73, D74 entries** — append-only ledger discipline. D75 EXTENDS the runtime semantics of D19 (specialist kind contract: real-wire `handle_skill` body) + ACTIVATES D50 §B.1 (SkillExecutionError forward-bar under real-wire conditions) + RESOLVES D50 §D D-5 (composition contract first concrete precedent: WRAP) without altering any of those entries' slot definitions or contract surfaces.
- **No change to the specialist kind contract slots** — D19 slot definitions stand. The 0.2.0 extension preserves the 0.1.0 vocabulary registration (`work-unit.kind` identifier `retrieval-task`) and adds a NEW specialist provision id under the existing slots; no schema amendment.
- **No new typed exception** — c7 reuses the D50-locked `SkillExecutionError` (introduced at D50 §B.1) + the D48-locked `AdapterCallError` (introduced at D48 §B.1; activated under real-wire at D71 §B.1). No new exception class introduced.
- **No new FAILURE_CATEGORIES entry** — server-side failures surface as AdapterCallError per D71 §B.1; specialist-body failures surface as SkillExecutionError per D50 §B.1. Neither uses the ValidationFailure FAILURE_CATEGORIES vocabulary extended by D46/D48; c7 honors the D50 separation explicitly.
- **No change to `_SPECIALIST_CLASSES` dispatch mechanism** — registry-by-spec-id at `specialist.py:654-658` stands. 0.2.0 introduces a new key (`rag-realwire-specialist`) alongside the existing two (`generic-specialist`, `rag-specialist`); the dispatch logic itself is unchanged.
- **No pyproject.toml dep additions** — the `mcp>=1.0` dep added at D71 is reused via the c3 RealWireMCPClientAdapter (which c7 invokes as the bound adapter); no c7-specific dep delta.
- **No 0.1.0 RAGSpecialist refactor** — per D41 two-version parity precedent (mcp-server-ext 0.1.0/0.2.0 coexistence at D71): the B7 `RAGSpecialist` stub at `specialist.py:338` stands unchanged for stub-test back-reference + the `rag-via-mcp-ext:rag-specialist` 0.1.0 provision continues to resolve to RAGSpecialist. c7 adds the 0.2.0 path alongside without modifying the 0.1.0 path.

## Decision-shape template self-application (per probing.md Procedure 1)

- **WHAT**: lock detection + surface + recovery for the RealWireRAGSpecialist handle_skill body's four-category failure classification (domain-error / external-dependency-error / skill-execution / unknown) under real-wire MCP retrieval flow + ship the RealWireRAGSpecialist impl + 0.2.0 extension + in-process self-test harness via `mcp.shared.memory.create_connected_server_and_client_session` + 10 tests; closes Phase C workstream C7 per D68 §A + §C closure items (c) + (f); ACTIVATES D50 §B.1 SkillExecutionError forward-bar under real-wire conditions; RESOLVES D50 §D D-5 (AdapterCallError-inside-handle_skill composition contract) as first concrete precedent: WRAP.
- **WHO**: enforced by *specialist (impl)* — `RealWireRAGSpecialist.handle_skill` body classifies failures via the four-category cascade + raises `SkillExecutionError` with structured detail. *adapter (runtime)* — `RealWireMCPClientAdapter.call` (D71) is the bound adapter raising `AdapterCallError` that c7 wraps. *substrate (runtime)* — `Specialist.attach_workspace` (specialist.py:197-286) resolves `required-adapter-bindings` to the c3 adapter binding + sets the emit-attribution closure (D64 §B.1). *extension (registered)* — `rag-via-mcp-ext/0.2.0` provisions the `rag-realwire-specialist` id; `_SPECIALIST_CLASSES["rag-realwire-specialist"]` dispatches at load time.
- **FAILS** (recursive — what happens if D75's contract isn't honored at impl?): *Detection*: detection-surface-recovery audit at workstream-completion (Cref) + phase-boundary checkpoints catches any path where (a) AdapterCallError leaks un-wrapped from handle_skill (D-5 RESOLUTION not honored — propagate-raw instead of WRAP without explicit docstring statement); (b) domain validation skipped → garbage params reach adapter.call; (c) result-parsing invariant violations classify as `unknown` instead of `skill-execution` (category-vocabulary discipline violation); (d) D64 §B.1 emit-attribution not inherited (RealWireRAGSpecialist overrides attach_workspace without preserving the closure-wrap). *Surface*: audit findings list; clippy:reviewer findings on impl commits. *Recovery*: clarification entry sharpens contract OR impl-follow-through commit closes the gap.
- **CROSS**: D5 §I3 (accountability anchor — pre-wire action emit per D71 §B.1 records intent regardless of outcome); D10 (event chain — chain integrity preserved by pre-wire emit; failure paths leave the action event in the chain); D16 (adapter kind contract — c7 invokes the c3 D71 adapter; D75 does not modify D16 slot definitions); D19 (specialist kind contract — D75 extends runtime semantics; slot list stands; first real-wire specialist body per c7); D29 (extension manifest + vocabulary namespacing — 0.2.0 preserves 0.1.0 work-unit.kind registration + adds new specialist provision id); D37 (event-driven coordination — orthogonal; c7's on_event is the inherited no-op default; cross-specialist coordination is Phase D+ territory); D38 (knowledge composes via existing primitives — c7 is the first real-wire activation of this thesis: retrieval specialist + MCP retriever adapter composed via D19/D16 primitives); D41 (extension-alongside parity — D75 adds `rag-via-mcp-ext/0.2.0` alongside preserved 0.1.0 stub; specialist.py keeps RAGSpecialist + adds RealWireRAGSpecialist); D44 (no async substrate model — c7 honors via the c3 D71 adapter's `asyncio.run` sync-wrap; specialist body is synchronous); D45 (standing requirement for runtime triad — D75 §B.1 honors); D46 (`WorkspaceBootError` pattern — REUSED via Specialist.attach_workspace adapter-binding-resolution); D47 §B.1 (SubscriberDispatchError aggregation — orthogonal at c7; on_event is no-op; future on_event-delegating-to-handle_skill compositions will exercise D50 §B.2 indirect-path surface); D48 §B.1 (AdapterCallError — c7 catches + WRAPs per D50 §D D-5 RESOLUTION); D48 §B.2 (adapter-attach — REUSED via Specialist.attach_workspace adapter-binding-resolution); **D50 §B.1 (SkillExecutionError forward-bar — D75 ACTIVATES first under real-wire conditions); D50 §D D-5 (composition contract — D75 RESOLVES as first concrete precedent: WRAP)**; D57 §B.1 (configuration pass-through — c7 ships with no configuration slots; future per-shape configuration extensions register per D29); D58 (work-unit lifecycle derivation — orthogonal; c7 doesn't manipulate work units); D60 (specialist.skills[].publicly-exposed slot — orthogonal; c7's `retrieve` skill is not registered as publicly-exposed because c7's scope is not server-side MCP exposure; if Phase D pioneer-instance binds c7 specialist to an MCPServerAdapter D74 for outbound MCP exposure, it MAY set publicly-exposed=true via a per-shape override); **D64 §B.1 (specialist emit-attribution — D75 INHERITS via Specialist.attach_workspace closure-wrap unchanged; tests verify emitting-specialist field present)**; D66 (PreToolUse hook structural enforcement — orthogonal to runtime); D68 §A + §C closure items (c) + (f) + §E scope-cut (Phase C workstream C7 — D75 closes; closure items (c) + (f) in-process harness per scope-cut C12); D69 (Phase C C1 substrate — D75 plugs alongside; cross-impl orthogonal); D70 (Phase C C2 persistence — c7's specialist-emitted action events traverse Step 4a write-through unchanged when persistence is configured); **D71 (Phase C C3 real-wire MCP CLIENT adapter — DIRECT dependency: c7 invokes RealWireMCPClientAdapter end-to-end through the bound adapter binding; D71's starter category vocabulary is the source of the AdapterCallError surface that c7 WRAPs)**; D72 (Phase C C4 real-wire direct-API HTTP adapter — cited as parallel adapter-side pattern; orthogonal to c7's specialist focus); D73 (Phase C C5 real-wire A2A peer adapter — parallel pattern; closes closure item (a); orthogonal to c7's specialist focus); D74 (Phase C C6 real-wire MCP server adapter — parallel adapter-side pattern; closes closure item (b); orthogonal to c7's specialist focus); probing.md Procedure 1 + Procedure 3 refined-skip rule (FIRED disposition per NEW contract content rule).
- **DEFERS**: per §D — D-1 real-corpus retrieval (real embeddings + vector DB + document store); D-2 per-shape SkillExecutionError category vocabulary extensions; D-3 streaming retrieval / batch retrieve / cross-corpus federation; D-4 specialist-side caching / deduplication / result post-processing; D-5 generic-specialist real-wire equivalent.

## E. Pre-lock probe disposition (per probing.md Procedure 3 refined-skip rule)

**FIRED** per refined-skip rule + D48 §E + D50 §E + D71 §E precedent cluster: D75 introduces NEW contract content beyond pure pattern application of prior real-wire workstreams. Specifically:

1. D50 §B.1 SkillExecutionError forward-bar ACTIVATION under real-wire conditions — first concrete activation of the forward-bar locked at D50 (the B6/B7 stubs do not trigger SkillExecutionError per D50 §D D-1; c7 is the first impl that DOES).
2. D50 §D D-5 composition contract RESOLUTION as first concrete precedent — D50 deferred the AdapterCallError-inside-handle_skill composition choice ("first concrete real-wire impl establishes precedent"); D75 chooses WRAP and locks the precedent. Future specialists MAY choose PROPAGATE-RAW per the explicit D50 §D D-5 framing; D75 is not exclusive but is the first concrete answer.
3. End-to-end composition of two typed-exception flows (D48 §B.1 AdapterCallError + D50 §B.1 SkillExecutionError) under a single real-wire invocation — the cross-exception-type chain (`__cause__` link from SkillExecutionError to AdapterCallError) is exercised end-to-end for the first time at c7.

Probe brief shape: V1 code-claim verification + Phase C / D quiet-assumption surfacing (per probing.md Procedure 3 brief menu). Investigation-before-claim discipline applied at pre-impl + during impl:

- **Pre-impl code-claim verification** caught two empirical corrections from the tracker draft:
  1. Tracker's `required-adapter-bindings=['mcp-server-ext:mcp-client-realwire-adapter']` was wrong — the c3 adapter provision id is `mcp-real-wire-adapter` per `extensions/mcp-server-ext/0.2.0/adapter.json:2` (the `protocol-or-transport` identifier is `mcp-client-realwire` but that's the protocol slot, not the adapter provision id). Correct binding string: `mcp-server-ext:mcp-real-wire-adapter` (verified via `grep -rn "required-adapter-bindings" impl/` + reading `boot.py:444-447` `adapter-bindings[].provision` field used by `Specialist.attach_workspace` resolution at `specialist.py:262-267`).
  2. Tracker's shape role-id assumption — generic-shape's role list (`author`, `reviewer`) does not include `retriever`; the B7 RAG fixture had to extend the shape with `retriever` role for c7. Caught at first boot attempt (WorkspaceBootError `identity` category); resolved by mirroring the B7 fixture's shape.json extension.

These corrections are consistent with the c1-c6 pattern (investigation-before-claim catches tracker-draft drift; pattern stable across Phase C).

Quiet assumptions surfaced + named as §D explicit deferrals:

- D-1 real-corpus retrieval — c7 ships synthetic retrieval per scope-cut C12; production real-corpus integration deferred.
- D-2 per-shape category vocabulary — c7 commits the practitioner-shape-flavored starter set per D50 §B.1; per-shape extensions register per D29.
- D-3 streaming / batch / federation — c7 ships request/response single-call shape; richer shapes deferred.
- D-4 specialist-side caching / result post-processing — c7 ships stateless body; shape-policy or specialist-internal-policy concern.
- D-5 generic-specialist real-wire equivalent — c7 closes RAG specialist real-wire; generic-specialist real-wire is a parallel pattern deferred to Phase D pioneer-instance.

Probe disposition: D75 §B.1 + §D content represents the probe-validated lock. No T1 architectural REVISION surfaced; T2/T3 findings absorbed into §D explicit deferrals.

## Rationale

Per the durability bet (CONCEPTS "What is durable vs scaffolding"): D75 is the first concrete activation of two forward-bars locked specification-only at Phase B (D50 §B.1 SkillExecutionError + D50 §D D-5 composition). The locks chosen at Phase B (typed exception + category surface + structured field shape + composition-choice framing) survive Phase C ACTIVATION without re-litigation — exactly the durability property the spec-vs-impl boundary at D50 aimed for. The WRAP choice at D-5 RESOLUTION reflects the framework-level cost model (uniform skill-failure surface > layer-error transparency for caller-policy simplicity) but explicitly preserves the PROPAGATE-RAW option per D50 §D D-5 framing for specialists that prefer transparency.

The pattern composition is HONEST: c7 reuses D71 c3 adapter UNCHANGED + D50 typed-exception UNCHANGED + D64 emit-attribution closure-wrap UNCHANGED + D41 two-version parity precedent UNCHANGED. The NEW content is the COMPOSITION itself — first real-wire specialist body invoking real-wire adapter under real-wire MCP transport — plus the explicit D-5 resolution choice. No primitive vocabulary is introduced; no schema slot is amended; no FAILURE_CATEGORIES entry is added. The framework's specialist + adapter + extension primitives carry the load.

**Cross-references**: D5 §I3 (accountability anchor — pre-wire action emit + emitting-specialist attribution); D10 (event chain — chain integrity preserved); D16 (adapter kind contract — c3 D71 adapter); D19 (specialist kind contract — D75 extends runtime semantics); D29 (extension manifest + vocabulary namespacing); D37 (event-driven coordination — on_event no-op inherited); D38 (knowledge composes via existing primitives — D75 first real-wire activation); D41 (extension-alongside parity — 0.2.0 added alongside preserved 0.1.0 stub); D44 (no async substrate model — synchronous specialist body); D45 (standing requirement; canonical citation); D46-D48 (adapter cluster + adapter-attach reuse); D50 §B.1 + §B.2 + §D D-5 (D75 ACTIVATES + RESOLVES); D57 §B.1 (configuration pass-through — no c7-specific slots); D60 (publicly-exposed orthogonal at c7); D64 §B.1 (emit-attribution INHERITED — D75 verifies tests); D68 §A C7 + §C closure items (c) + (f) + §E scope-cut C12; D69-D74 (parallel Phase C workstreams; orthogonal); D71 (DIRECT dependency: c3 adapter invoked end-to-end); probing.md Procedure 3 refined-skip rule (D75 §E FIRED disposition).
