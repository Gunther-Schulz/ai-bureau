# DR: quality-gate ARCH topic — Pattern A 12+7 third instance + composite Mode 2 decomposition

## 1. Status

**ACCEPTED** (composite DR Mode 2 upfront-known decomposition; 6 sub-decisions visible at framing)

**Locked**: Phase 3.6 (sole ARCH topic; closes Phase 3.6 1 of 1)

**Sharpening**: 2-round pattern (Round 1 full monty + Round 2 user-triggered) under G + multi-axis + D gate disciplines per `DISCIPLINES.md` Discipline 3 + 2-round sweet spot empirical pattern

## 2. Owner

Phase 3.6 — quality-gate ARCH topic; Pattern A 12+7 third + final instance per Phase 3 ARCH catalog (substrate anchor + adapter second + quality-gate third); Pattern A topic-template-class FORMAL STABILITY achieved with this lock.

## 3. Related

**Composes-with DRs**:
- `docs/decisions/quality-gate-scope-lock.md` — Phase 2 scope-lock (Pattern A protocol with mechanism-shaped Surface; Hybrid Option C; per-shape implementations; per-shape error semantics)
- `docs/decisions/substrate-arch-topic.md` — Phase 3.4 first Pattern A topic (12+7 template anchor)
- `docs/decisions/adapter-arch-topic.md` — Phase 3.4 second Pattern A topic (12+7 extends WITHOUT variation)
- `docs/decisions/sparring-arch-topic.md` — Phase 3.4 mechanism-class topic (axis-2 observability source for gate)
- `docs/decisions/audit-arch-topic.md` — Phase 3.4 mechanism-class topic (gate emits via Surface §A; gate state-restore reads via Surface §C; per-shape audit emission granularity composes with gate per-shape variation)
- `docs/decisions/axis-interactions-arch-topic.md` — Phase 3.5 sixth + final ARCH topic (cross-axis category-collapse counter-mechanism per §4.3; resolves §7 E2 + §14 W4 forward-references)
- `docs/decisions/specialist-skill-arch-topic.md` — Phase 3.5 first primitive-cluster topic (per-skill gate-firing requirements per E8)
- `docs/decisions/practitioner-arch-topic.md` — Phase 3.5 second primitive-cluster topic (HITL approval moments via substrate Surface §C)
- `docs/decisions/workflow-work-unit-arch-topic.md` — Phase 3.5 third primitive-cluster topic (workflow_instance + work-unit observability sources)
- `docs/decisions/claim-defensibility-arch-topic.md` — Phase 3.5 fourth primitive-cluster topic (per-claim attestation chain IS gate's axis-3 observability source)
- `docs/decisions/scope-model-arch-topic.md` — Phase 3.5 fifth ARCH topic (cross-cutting integrator anchor; gate Implementation Framework C + Running Instance Owner B)
- `docs/decisions/pattern-a-template-7th-conditional-cross-shape-variation.md` — per-pattern instance-driven trigger pattern (12+7 template extends WITHOUT variation across Pattern A instances)
- `docs/decisions/greenfield-rederivation-pause.md` — Phase 3 ARCH catalog rederivation (Pattern A 3-instance catalog complete)

**GLOSSARY entries (20+)**: `quality-gate` (canonical PRIMITIVE Pattern A) + `protocol (architectural)` (Pattern A meta-primitive) + `category-collapse` (cross-axis force) + 3 axes (intertwining + sparring + authorship-preservation) + axis-1 frames (co-worker / intertwined-ai / tacked-on-ai) + axis-2 failure modes (answer-machine / oracle / validator) + axis-3 success/failure (engaged-authorship / rubber-stamping / defensibility) + cross-axis attribution (authority-binding) + composing primitives (claim / workflow / work-unit / session / event / actor / framework / mechanism / Framework C scope / Owner B scope / workspace / shape / specialist / skill / practitioner / substrate / adapter)

**Disciplines applied**: per `DISCIPLINES.md` Discipline 1 (source-grounded; cite file:line; flag synthesis vs citation; re-grounding sub-section + skill+profile sub-section); Discipline 3 (pre-decision sharpening; Round 1 full monty + Round 2 user-triggered; multi-axis validation sub-section + profile-anchored validation sub-section); Discipline 4 (cascade-prevention; greenfield-draft + minimize-embedded + cascade-pass + foundation-first); Discipline 5 (no-defer; mental-model first; surface info-gaps as watch-list entries); Discipline 8 (foundation-up workflow ordering); Discipline 10 (greenfield-evaluation of archived material — NO archive territory)

## 4. Context

Phase 3.6 single-topic phase per `ARCHITECTURE.md` §2 sub-phase status. Phase 3.5 closed (6 of 6 ARCH topics LOCKED) per `ARCHITECTURE.md` §2; coherence-audit checkpoint C2 CLOSED per `disciplines/09-coherence-audit-cadence.md` cadence + HANDOFF Note 62.

Quality-gate scope-lock LOCKED Phase 2 per `docs/decisions/quality-gate-scope-lock.md`: type classification = Pattern A protocol with mechanism-shaped Surface (Hybrid Option C); per-axis-failure-mode signal catalog; per-shape implementation variation; fail-closed/open semantics per shape. Phase 3.6 ARCH topic locks WHAT (architectural shape) per scope-lock DR §Defers ("Surface specification specifics; Per-implementation detail; Specific observability signals + thresholds; Specific intervention mechanics; Error-semantics specifics; Tier-awareness configuration" → Phase 3.6 ARCH topic).

This topic is the THIRD + FINAL Pattern A protocol instance per Phase 3 ARCH catalog (substrate anchor Phase 3.4 + adapter second Phase 3.4 + quality-gate third Phase 3.6). Pattern A topic-template-class FORMAL STABILITY achieved with three instances complete per per-pattern instance-driven trigger pattern per `docs/decisions/pattern-a-template-7th-conditional-cross-shape-variation.md`.

Cross-axis category-collapse counter-mechanism: quality-gate is the architectural counter-mechanism for category-collapse per `arch/axis-interactions.md` §4.3 (gate fires at runtime checkpoints to monitor for category-collapse manifestations across all 3 axes per CC-1 cascade pattern per `arch/axis-interactions.md` §3.4; surface drift signals; intervene per shape policy). Resolves `arch/axis-interactions.md` §7 E2 per-axis observability hook signal-set forward-reference + §14 W4 quality-gate cross-axis signal-set integration watch-list (this topic IS the W4 resolution mechanism).

The decision-design phase needs to resolve: Pattern A 12+7 template extension verdict (extends WITHOUT variation per per-pattern trigger; 4 conditional applies + 3 N/A documented); Surface contract specifics (single-layer mechanism-shaped per Hybrid Option C; 6 capability categories); per-implementation aspect + selection mechanics + tri-aspect reconciliation; cardinality + lifecycle + boot/shutdown phase ordering integration with composite sequence; per-shape error categories + tier-awareness + cross-shape policy variation 6-row matrix; composition with framework primitives + W1-W4 watch-list + cross-references + phase routing.

## 5. Decision

Six sub-decisions encoded (Mode 2 upfront-known composite decomposition per `decision-design-sharpening` v0.6.0 composite decomposition pattern + `arch/specialist-skill.md` 6 sub-decisions precedent + `arch/practitioner.md` 5 sub-decisions precedent + `arch/workflow-work-unit.md` 6 sub-decisions precedent + `arch/claim-defensibility.md` 6 sub-decisions precedent + `arch/scope-model.md` 6 sub-decisions precedent + `arch/axis-interactions.md` 6 sub-decisions precedent).

### Sub-decision 1: Pattern A 12+7 template extends WITHOUT variation (third Pattern A instance)

Per per-pattern instance-driven trigger pattern per `docs/decisions/pattern-a-template-7th-conditional-cross-shape-variation.md`. Per-pattern conditional applicability for quality-gate:

- §3 Common-surface boundary criteria: **N/A** (single-layer Surface per SD-2; document N/A explicitly per Pattern A template; parallel to substrate §3 N/A precedent)
- §8 Substrate-internal vs skill-side audit emission: **N/A** (substrate-specific to substrate Pattern A; quality-gate emits skill-side via audit Surface §A; document N/A explicitly; parallel to adapter §8 N/A precedent)
- §10 Boot + shutdown phase ordering: **APPLIES** (gate state-restore from audit-trail at boot; cumulative engagement signals integrity invariants for stateful impls; cross-axis composite boot integration with `ARCHITECTURE.md` §6 composite sequence)
- §11 Substrate error categories: **APPLIES** (per-shape error semantics fail-closed/open variations + per-impl error categories: GateUnreachable / SignalIngestionFailure / SignalEvaluationFailure / InterventionDispatchFailure / GateStateRestoreFailure / EventEmissionFailure)
- §12 Transport variation + per-tier mapping: **N/A** (gate substrate-agnostic; document N/A explicitly; parallel to adapter §12 N/A precedent)
- §13 Deployment-tier awareness: **APPLIES** (per-tier intervention mechanics + multi-tenant gate state isolation via session_id + actor_id discrimination)
- §14 Cross-shape policy variation: **APPLIES** (PRIMARY conditional; quality-gate IS shape-policy-mediated by definition per scope-lock DR Hybrid Option C; 6-row matrix)

→ **17 sections total** (12 common + 4 conditional applies + 3 N/A documented).

Pattern A topic-template-class FORMAL STABILITY achieved with three instances complete (substrate anchor + adapter second + quality-gate third); 12+7 extends WITHOUT variation across all three instances per per-pattern instance-driven trigger pattern (parallel to cross-cutting integrator FORMAL STABILITY language in `arch/axis-interactions.md` per Note 61). Pattern A instances complete with Phase 3.6 close (3 of 3 — substrate anchor + adapter + quality-gate; this is the LAST Pattern A topic per Phase 3 ARCH catalog).

### Sub-decision 2: Surface contract — single-layer mechanism-shaped; 6 capability categories

Per `glossary/quality-gate.md` mechanism-shaped Surface + scope-lock DR Hybrid Option C. Single-layer (NOT multi-class per-axis). 6 capability categories:

- **A. Checkpoint firing API** — `gate.fire(checkpoint_kind, context)` → `GateDecision`. checkpoint_kind enum two-class taxonomy: event-triggered (`pre_send` / `pre_claim_finalization` / `pre_decision_lock` / `per_edit` / `workflow_phase_transition` / `session_end`) + periodic/threshold-triggered (`drift_audit`)
- **B. Per-axis signal ingestion** — `gate.ingest_signal(axis_kind, signal_kind, value, source_event_ref)`. Per-axis-failure-mode signal catalog (axis-1 tacked-on detection / axis-2 oracle-validator-answer-machine extraction patterns / axis-3 rubber-stamping detection); resolves `arch/axis-interactions.md` §7 E2
- **C. Signal evaluation** — `gate.evaluate(checkpoint_kind, signal_set, threshold_set)` → engagement-quality verdict. Per-shape impl declares thresholds + evaluation logic. Cumulative observability across axes per CC-1 cross-axis cascade pattern requirement
- **D. Intervention dispatch** — `gate.intervene(decision, intervention_kind, context)`. intervention_kind ∈ {`friction` / `nudge` / `block` / `audit_only`}. When intervention_kind = `block` → authority-bound denial via substrate Surface §C `request_permission`; friction + nudge are skill-side direct emission. Authority-binding records `actor_kind: ai_runtime`
- **E. Audit emission** — gate-fired events via audit Surface §A. Event-kinds candidate: `gate_fired` / `gate_intervention_applied` / `gate_threshold_crossed` / `gate_state_persisted` / `gate_state_restored` / `gate_active`. Skill-side emission via MCP audit gate (parallel to adapter §8 + sparring §8)
- **F. State management** — stateful vs stateless choice per-impl. Audit-trail IS state-store: `gate.get_state()` reads via audit Surface §C query API; `gate.set_state()` IS `gate_state_persisted` emission via Surface §A. NO separate gate-state-store. Avoids dual-store divergence; preserves single-write architecture per `arch/audit.md` §10 audit-trail-as-canonical-source. Stateless impls: no state read/write semantics on the Surface

Phase 6 lands typed Pydantic Protocol + per-axis observability hook signal-set spec.

### Sub-decision 3: Per-implementation aspect (§4) + selection mechanics (§5) + tri-aspect reconciliation (§6)

Implementations: practitioner-shape-gate / autonomous-business-shape-gate / personal-OS-shape-gate / extensible (research-lab-shape-gate at second-shape productization per W1).

Per-impl declarations (parallel to substrate §4 + adapter §4):
- Implementation identity (id; e.g., practitioner_shape_gate)
- Surface satisfaction (claim + impl mapping each capability category to per-shape native impl)
- Per-axis signal threshold declarations
- Per-shape intervention mechanics specifics (per-shape policy variation §14)
- Per-shape error semantics (fail-closed / fail-open with alert / fail-open per shape)
- State-management mode (stateful vs stateless per audit-trail-as-state-store reframe)
- Deployment-tier compatibility (per §13)
- Per-shape extension Protocols (forward-reference Phase 6 spec; e.g., practitioner-shape-gate extension exposes practitioner-attestation-extension; autonomous-business-shape-gate extension exposes budget-policy-extension; pattern parallel to `arch/substrate.md` §4 per-substrate extension Protocols pattern; document forward-reference at §15 NOT lock at this topic)

**Selection mechanics (§5)** — shape-mediated, NOT direct workspace.md selection. Workspace selects shape via `workspace.md` `shape:` field; transitive gate-impl selection through shape policy bundle declaration. 1:1 cardinality (1 active gate impl per workspace per `glossary/quality-gate.md`). Validation at workspace boot: shape policy bundle's gate-impl declaration resolves to known Implementation; deployment-tier compatibility verified; gate-impl compatible with active substrate (gate substrate-agnostic — should always pass; document anyway for completeness).

**Tri-aspect reconciliation (§6)** — Surface (mechanism-shaped per SD-2) + Implementations (shape-policy-distributable per SD-3) + Running Instance (workspace-bound Owner B; 1 per workspace). Gate-coupling impossible-by-construction at Surface level (Surface-typed skill code calling `gate.fire / gate.ingest_signal / gate.evaluate / gate.intervene / gate.emit_event / gate.get_state` cannot reach impl-specific intervention mechanics without explicit isinstance check on per-shape Extension Protocol per Phase 6).

### Sub-decision 4: §9 cardinality + lifecycle + §10 boot/shutdown ordering integration with composite sequence

§9 cardinality table (parallel to substrate §9 + adapter §9):

| Concern | Value | Mechanism |
|---|---|---|
| Quality-gate Implementations per workspace | 1 | Shape-mediated selection per shape policy bundle declaration; framework-level 1:1 with workspace |
| Running Instance per workspace | 1 | Bound at workspace boot; one active gate impl |
| Implementations per Framework C catalog | N (current 3 + extensible) | Multiple distributable definitions; current set: practitioner-shape-gate / autonomous-business-shape-gate / personal-OS-shape-gate |
| gate-firing events per workspace lifetime | N | Per-checkpoint events (pre_send / pre_claim_finalization / etc.); event-triggered + periodic/threshold-triggered |

Lifecycle ownership: Creator = framework-runtime (workspace activation orchestrator); Owner = workspace deployment (Owner B); Destroyer = framework-runtime at workspace shutdown.

Lifecycle distinction: gate-active is workspace-lifetime state (post-boot-phase-4); gate-firing is per-checkpoint event lifecycle within gate-active. Document distinction explicitly.

State persistence: stateful impls persist state IN audit-trail (no separate store); stateless impls have no persistence. Cross-deployment portability composes via audit-trail portability (W3 mechanism = audit-trail portability includes gate-state events).

§10 boot ordering — gate boot fires AFTER substrate-phase 5 `boot_complete` (parallel to substrate §10 + audit §10 phase ordering). REQUIRES `ARCHITECTURE.md` §6 Workspace boot+shutdown composite sequence amendment: add gate-phase 1-4 after substrate-phase 5 + corresponding shutdown ordering.

**Boot sequence (architectural ordering)**:
- Precondition: audit-phase 1-3 ready (audit Surface available for gate emission); substrate-phase 1-5 complete (specialists registered → axis-1 mechanisms operational; sparring sub-mechanisms via skills → axis-2 observability; per-claim attestation chain available → axis-3 observability)
- gate-phase 1: gate Implementation instantiates per shape policy bundle declaration
- gate-phase 2: gate state-restore from audit-trail (stateful impls reads prior `gate_state_persisted` events via audit Surface §C; reconstructs cumulative engagement signals; emits `gate_state_restored` upon completion)
- gate-phase 3: gate registers checkpoint-firing handlers (subscribes to per-axis observability hooks per `arch/axis-interactions.md` §7 E2; checkpoint subscriptions for event-triggered + periodic/threshold-triggered classes)
- gate-phase 4: gate emits `gate_active` event via audit Surface; gate ready to fire

**Shutdown sequence** — gate shuts down AFTER substrate but BEFORE audit storage realization (parallel to sparring §10 mechanism-class shutdown ordering):
- gate-shutdown 1: gate emits final drift report event
- gate-shutdown 2: gate state persistence — stateful impls flush cumulative signals via `gate_state_persisted` event emission to audit Surface §A
- gate-shutdown 3: gate unsubscribes from observability hooks
- gate-shutdown 4: gate shutdown returns; audit storage realization shutdown follows per `arch/audit.md` §10

**Cross-axis dependency** (boot integration per axis-interactions §3.5): gate consumes per-axis observability — all 3 axes operational + AuditEvent schema + audit Surface ready before gate-phase 1.

### Sub-decision 5: §11 error categories + §13 tier-awareness + §14 cross-shape policy variation 6-row matrix

**§11 error categories** (parallel to substrate §11 + adapter §11):
- `GateUnreachable` — gate impl unreachable; per-shape fail-closed/open semantics
- `SignalIngestionFailure` — per-axis signal ingestion failure
- `SignalEvaluationFailure` — threshold evaluation failure
- `InterventionDispatchFailure` — intervention mechanics failure (friction not displayed / nudge not delivered / block not enforced via substrate Surface §C); per-shape escalation semantics
- `GateStateRestoreFailure` — boot-time state-restore failure for stateful impls (audit-trail integrity violation; missing prior `gate_state_persisted` event; per-shape continuation semantics)
- `EventEmissionFailure` — gate-fired event emission failure; composes with audit Surface §11 error categories

Per-shape error semantics:
- practitioner-shape-gate: fail-closed (defensibility-critical)
- autonomous-business-shape-gate: fail-open with alert (continuity prioritized)
- personal-OS-shape-gate: fail-open (lightweight)

**§13 deployment-tier awareness** — per-tier behavior in impl, NOT Surface (parallel to substrate §13):
- Tier 1 local: full gate enforcement default; possible advisory-mode for development workflows
- Tier 2 cloud small-firm: full gate enforcement; multi-tenant gate state per tenant via session_id + actor_id discrimination
- Tier 3 federated enterprise: per-tenant isolation strict; gate state federation considerations cross-link W3 cross-deployment portability

**§14 cross-shape policy variation** — 6-row matrix (parallel to scope-model §8 + axis-interactions §8 + claim-defensibility §8 + workflow-work-unit §8 6-row precedents):

| Dimension | practitioner-shape | autonomous-business-shape | research-lab-shape (preliminary) | personal-OS-shape |
|---|---|---|---|---|
| Gate firing checkpoints | pre_send + pre_claim_finalization + per_edit + drift_audit (full enforcement) | autonomous-decision-checkpoints + budget-policy thresholds (drift_audit periodic) | pre_send + pre_claim_finalization + co-attestation checkpoints | session_end + drift_audit (light) |
| Per-axis signal thresholds | strict per-axis (axis-3 PRIMARY; rubber-stamping detection mandatory) | budget-policy substitutes per-claim (axis-2 PRIMARY) | strict + multi-author co-attestation thresholds | minimal advisory thresholds |
| Intervention mechanics | friction + nudge + block + practitioner attestation + re-engagement | programmatic-block + audit-only + budget-alert | friction + nudge + co-attestation requests + block | audit-only + drift-check report |
| Error semantics | fail-closed (defensibility-critical) | fail-open with alert (continuity prioritized) | fail-closed (research output accountability-bearing) | fail-open (lightweight) |
| State management | stateful (cumulative engagement signals across session via audit-trail) | stateless (per-checkpoint policy evaluation) | stateful (multi-author engagement signals via audit-trail) | stateful but lightweight (drift-check accumulation via audit-trail) |
| Per-shape failure-mode salience | rubber-stamping deadline-pressure (axis-3); answer-machine regulatory transcription (axis-2); cross-link `arch/axis-interactions.md` §8 row 4 | category-collapse to feature-catalog (axis-1) | answer-machine literature review (axis-2); rubber-stamping co-attestation drift (axis-3) | tacked-on-AI default (axis-1) |

Cross-link explicit at §14 between this row 6 + `arch/axis-interactions.md` §8 row 4+5.

Resolves `arch/axis-interactions.md` §14 W4 (quality-gate cross-axis signal-set integration); composes with axis-interactions §8 6-row per-shape cross-axis activation matrix.

### Sub-decision 6: §7 composition + §16 W1-W4 watch-list + §17 cross-references + §18 phase routing

§7 composition with framework primitives (parallel to substrate §7 + adapter §7) — table covering all primitives this topic composes with: framework / mechanism / Framework C scope / Owner B scope / protocol (architectural) / actor / category-collapse (cross-axis force) / 3 axes (axis-1 / axis-2 / axis-3) / sparring (mechanism class) / audit (mechanism class) / engaged-authorship / defensibility / claim / workflow_instance / work-unit / session / event / authority-binding / shape / specialist / skill / practitioner / substrate / adapter / scope-model / axis-interactions.

§16 W1-W4 watch-list (parallel to prior cluster precedents):
- **W1**: Second-shape productization quality-gate variation (autonomous-business / personal-OS / research-lab concrete impls) — awaits second-shape design begin per BACKLOG shape-neutrality watch-list + `arch/scope-model.md` §14 W4 + `arch/axis-interactions.md` §14 W1
- **W2**: Per-axis observability hook signal-set + threshold-set evidence accumulation — awaits Phase 1+ pioneer deployment data; resolves `arch/axis-interactions.md` §7 E2 Phase 6 spec target
- **W3**: Cross-deployment gate-state portability via audit-trail composition — awaits first cross-deployment claim ingestion deployment evidence; cross-link `arch/practitioner.md` W1 + `arch/scope-model.md` W1+W2 + `arch/substrate.md` §F + `arch/audit.md` §G
- **W4**: Quality-gate intervention efficacy empirical evidence (fail-closed vs fail-open per-shape thresholds; intervention mechanics efficacy by archetype) — awaits Phase 1+ regulatory-challenge outcomes per `VISION.md` §Falsification criteria

§15 pre-implementation operational concerns (Phase 6 forward references): per-impl extension Protocols pattern; specific observability signals + thresholds per axis; specific intervention mechanics; error-semantics specifics; tier-awareness configuration specifics; specialist DEFINITION manifest schema `gate_firing_requirements` field.

§18 phase routing: architectural shape (this topic) Phase 3.6 LOCKED; Pydantic Protocol contract Phase 6 (Mode 3 spec); per-impl concrete impls Phase 6; per-axis observability hook signal-set spec Phase 6 (resolves axis-interactions §7 E2); cross-axis error catalog Phase 6 (resolves axis-interactions §7 E4); per-shape threshold-set tuning Phase 1+ pioneer deployment data per W2; specialist DEFINITION manifest schema `gate_firing_requirements` field Phase 6 spec.

§17 cross-references (per Pattern A template + Lens 5 v0.2.2 hygiene): GLOSSARY entries 20+ canonical entries (quality-gate primary + axis-related per axis-interactions cross-reference + category-collapse + protocol-architectural + authority-binding + engaged-authorship + defensibility); Disciplines per `MAINTENANCE.md` TOP-LEVEL DESIGN PRINCIPLES §1+§2+§3 + TOP-LEVEL SCOPE + `ARCHITECTURE.md` §6 cross-cutting principles + `DISCIPLINES.md` Discipline 1+4+8+10; Profiles validated `G-composability-gate.md` + `L5a-planner-pbs-schulz.md` + `L8-auditor-reviewer-posthoc.md` + Cluster A producer transitively-satisfied; ARCH topics composing substrate + adapter + sparring + audit + specialist-skill + practitioner + workflow-work-unit + claim-defensibility + scope-model + axis-interactions; Phase 6 spec target `docs/specs/quality-gate.md`; Archived sources NONE per Discipline 10 greenfield-evaluation rule.

## 6. Sharpening provenance

This section IS the canonical sharpening-provenance home per `MAINTENANCE.md` Lens 5 v0.2.2 carve-out (provenance lives in DR §6 + HANDOFF + git log + commit messages + BACKLOG watch-list; NOT in `arch/quality-gate.md` body).

### Round 1 EXPANSIONS (full monty)

6 SD positions surfaced + Pattern A 12+7 conditional applicability resolved:

- **SD-1 position**: Pattern A 12+7 template extends WITHOUT variation per per-pattern instance-driven trigger pattern; 4 conditional applies (§10 + §11 + §13 + §14) + 3 N/A documented (§3 + §8 + §12); Pattern A topic-template-class FORMAL STABILITY achieved with three instances complete
- **SD-2 position**: Surface contract single-layer mechanism-shaped per scope-lock DR Hybrid Option C; 6 capability categories (checkpoint firing API + per-axis signal ingestion + signal evaluation + intervention dispatch + audit emission + state management)
- **SD-3 position**: Per-implementation aspect (3 concrete impls + extensible) + selection mechanics shape-mediated NOT direct workspace.md selection + tri-aspect reconciliation gate-coupling impossible-by-construction
- **SD-4 position**: §9 cardinality singular per workspace + §10 boot/shutdown ordering integration with composite sequence (REQUIRES `ARCHITECTURE.md` §6 amendment for gate-phase 1-4)
- **SD-5 position**: §11 error categories (6 categories) + §13 tier-awareness per-tier behavior in impl + §14 cross-shape policy variation 6-row matrix (PRIMARY conditional)
- **SD-6 position**: §7 composition with framework primitives (covers all primitives) + §16 W1-W4 watch-list + §17 cross-references + §18 phase routing

### Round 2 EXPANSIONS (user-triggered)

9 surfacing operations:

- **E1 event-kind pair** — `gate_state_persisted` / `gate_state_restored` event-kind pair added to Surface §E audit emission catalog (preserves stateful impl state across deployments)
- **E2 audit-trail-as-state-store reframe** — Surface §F state management semantics reframed: audit-trail IS state-store; NO separate gate-state-store; preserves single-write architecture per `arch/audit.md` §10. REVISION-flavored EXPANSION (reframes Surface §F semantics)
- **E3 permission flow at intervention** — when intervention_kind = `block` → authority-bound denial via substrate Surface §C `request_permission`; friction + nudge are skill-side direct emission; clarifies authority composition with intervention dispatch
- **E4 gate-firing vs gate-active** — lifecycle distinction clarified: gate-active is workspace-lifetime state (post-boot-phase-4); gate-firing is per-checkpoint event lifecycle within gate-active
- **E5 per-shape error semantics** — fail-closed (practitioner-shape) / fail-open with alert (autonomous-business) / fail-open (personal-OS) per-shape error semantics surfaced (already in scope-lock DR; carries through to ARCH topic §11 + §14)
- **E6 cross-deployment via audit-trail** — cross-deployment gate-state portability composes via audit-trail portability per `arch/audit.md` §G external-format export — gate state events are first-class in audit-trail; cross-deployment migration preserves gate state per audit-trail integrity invariants
- **E7 per-shape salience cross-link** — explicit cross-link at §14 row 6 (per-shape failure-mode salience) between this topic + `arch/axis-interactions.md` §8 row 4+5 (per-shape failure-mode salience + per-shape category-collapse vulnerability)
- **E8 specialist per-skill gate-firing** — specialist DEFINITION manifest schema `gate_firing_requirements: optional` Phase 6 spec target; reciprocal symmetry already locked between `glossary/specialist.md` + `glossary/quality-gate.md`
- **E9 two-class checkpoint taxonomy** — checkpoint_kind enum re-categorized: event-triggered (`pre_send` / `pre_claim_finalization` / `pre_decision_lock` / `per_edit` / `workflow_phase_transition` / `session_end`) + periodic/threshold-triggered (`drift_audit`). REVISION-flavored EXPANSION (re-categorizes existing checkpoint enum)
- **E10 authority-binding completeness as gate condition** — pre_send checkpoint evaluates "all claims in send batch have `claim_attested` event with `actor_kind: human`" → if any claim lacks attestation → fail-closed block per shape semantics

### REVISIONS surfaced

0 architectural REVISIONS. 2 REVISION-flavored EXPANSIONS:
- E2 reframes Surface §F semantics (audit-trail-as-state-store reframe; no separate gate-state-store)
- E9 re-categorizes existing checkpoint enum (two-class taxonomy: event-triggered + periodic/threshold-triggered)

Cumulative-count update: 8 → 10 across 7 cluster-executions per BACKLOG cumulative-count freeze 2026-05-04 + signal-(a) DEFER WITH NAMED AWAIT-SIGNAL preserved per HANDOFF Note 62 C2 closure verdict.

### Manufactured-criticism rejections

5 manufactured-criticism rejections:
- **E5 manufactured-criticism rejection**: per-shape activation matrix as separate surface (not Pareto-improving) — per-shape variation lives at Implementation aspect §4 + cross-shape policy variation §14; separate matrix would duplicate without sharpening
- **SD-1 manufactured-criticism rejection**: template variation (no instance-driven trigger evidence) — Pattern A 12+7 template extends WITHOUT variation per per-pattern instance-driven trigger pattern per `docs/decisions/pattern-a-template-7th-conditional-cross-shape-variation.md`; per-pattern conditional applicability differs per topic-instance discriminators (substrate 6 of 7 / adapter 5 of 7 / quality-gate 4 of 7) but template structure shared
- **SD-2 manufactured-criticism rejection**: multi-class Surface per-axis (loses cross-axis cumulative observability per CC-1) — per scope-lock DR locked single mechanism-shaped Surface; per-axis variation belongs at signal-set level (Surface §B per-axis signal ingestion + §C per-axis threshold-set), NOT per-axis Surfaces. Multi-class Surface would lose cross-axis cumulative observability (axis-failure cascades per CC-1 require single observation point ingesting cross-axis signals into one cumulative state)
- **SD-3 manufactured-criticism rejection**: workspace.md direct gate selection (bypasses shape-policy semantics) — per scope-lock DR Hybrid Option C: per-shape variation IS structural; shape-mediated selection preserves shape-policy semantics; direct selection would let workspace bypass shape's per-shape-required gate impl
- **SD-4 manufactured-criticism rejection**: substrate-phase 4 boot timing (axis-1 observability incomplete) — gate consumes axis-1 observability (workflow_instance phase transitions emit events requiring specialists + adapter bindings); only complete at substrate-phase 5 per cross-axis cumulative observability requirement

### GLOSSARY back-check verdict

CLEAN — reciprocal symmetry already locked between `glossary/quality-gate.md` Composes-with rows + reciprocal entries. Verified pre-existing reciprocal symmetry per `glossary/quality-gate.md` Composes-with: `protocol (architectural)` / `category collapse` / `intertwining (axis 1)` / `sparring (axis 2)` / `authorship preservation (axis 3)` / `defensibility` / `engaged authorship` / axis-failure modes / `co-worker` / `claim` / `workflow_instance` / `session` / `event` / `audit` protocol / `authority-binding` / `mechanism` / `shape` / `specialist`. Cross-corpus sweep verification (within Wave-2 cascade per cascade discipline mandatory cross-corpus sweep at Phase 3.6 phase-set closure per `MAINTENANCE.md` Cascade 1 commit `f075927` structural amendment) — to be executed during Wave-2 cascade-pass.

### Profile-anchored validation

4 of 4 clusters PASS:

- **Cluster A producer (G-composability-gate.md fleshed)**: gate-impl distributable per Framework C catalog supports consulting / internal-firm-reuse / OSS / marketplace-future / backup-migration consumption modes; per-shape gate-impl distributable; cross-substrate compatibility verified — gate substrate-agnostic ensures gate-impl portable across substrates. L1 specialist creator + L2 shape definer + L3 deployment template creator + L9 shape catalog curator profiles transitively-satisfied via shape policy bundle declares gate-impl-id (per `glossary/shape.md`) + specialist DEFINITION may declare per-skill gate-firing requirements per E8
- **Cluster B deployer (L5a-planner-pbs-schulz.md fleshed as deployer-of-self)**: pioneer reality grounds practitioner-shape-gate via deadline-pressure rubber-stamping risk per profile lines 51-54 send-phase + answer-machine extraction risk regulatory transcription per profile axis-2 risk in research phase. L4a workspace deployer solo + L4b workspace deployer firm-IT skeletons transitively-satisfied via workspace.md shape selection mechanism + shape policy bundle declaring gate-impl-id
- **Cluster C consumer (L5a-planner-pbs-schulz.md fleshed)**: planner archetype hybrid moments line 67 "occasional rubber-stamp risk during deadline-pressure send (axis 3 risk)" validates gate fires at pre_send checkpoint per Surface §A + per-axis signal threshold strict per-axis (axis-3 PRIMARY rubber-stamping detection mandatory); answer-machine RISK regulatory transcription validates axis-2 signal thresholds. L5b legal practitioner + L5c researcher + L5d auditor practitioner + L5e autonomous-business operator + L5f personal-OS knowledge worker skeletons transitively-satisfied via cross-archetype illustration in `glossary/quality-gate.md` (planner / lawyer / researcher / autonomous-business / personal-OS) anchoring framework neutrality
- **Cluster D validator (L8-auditor-reviewer-posthoc.md skeleton intended-exemplify content)**: defensibility verification post-hoc engagement validates gate's axis-3 intervention applies defensibility test at attestation moments per `glossary/defensibility.md`; per-claim attestation chain reconstructable via audit-trail per `arch/audit.md` §C query API validates Surface §B axis-3 signal catalog reasoning-chain reconstructability gaps. G + D gates fired — G transitively-satisfied via per-shape gate-impl distributable per Framework C catalog; D Gate satisfied per W1 + W2 + W3 + W4 awaited evidence (named external signals)

### Decomposition mode

Mode 2 upfront-known composite (6 SDs visible at framing per established Phase 3.5 cluster precedent — `arch/specialist-skill.md` 6 SDs + `arch/practitioner.md` 5 SDs + `arch/workflow-work-unit.md` 6 SDs + `arch/claim-defensibility.md` 6 SDs + `arch/scope-model.md` 6 SDs + `arch/axis-interactions.md` 6 SDs); foundation-up dependency edges between SDs:

- SD-1 (template) → foundation for all subsequent SDs
- SD-2 (Surface contract) → SD-3 (per-impl aspect satisfies Surface)
- SD-3 (per-impl + selection + tri-aspect) → SD-4 (cardinality + lifecycle bound at workspace boot per shape-mediated selection)
- SD-4 (cardinality + boot/shutdown) → SD-5 (error categories per-shape; tier-awareness per-impl; cross-shape variation 6-row matrix)
- SD-5 (per-shape variation) → SD-6 (composition + watch-list + phase routing)

## 7. Composition with existing architecture

This decision composes with all 9 prior locked Phase 3.4 + Phase 3.5 ARCH topics + locked GLOSSARY entries (Phase 2):

- **Pattern A topic-template-class FORMAL STABILITY achieved with 3 of 3 Pattern A instances complete** (substrate anchor + adapter second + quality-gate third per per-pattern instance-driven trigger pattern). Pattern A instances complete with Phase 3.6 close.
- **`ARCHITECTURE.md` §6 amendment for gate-phase composite boot+shutdown sequence** — adds gate-phase 1-4 after substrate-phase 5 + corresponding shutdown ordering (gate-shutdown 1-4 between sparring shutdown + audit storage realization shutdown).
- **Resolves `arch/axis-interactions.md` §7 E2 + §14 W4 forward-references** — quality-gate is the W4 resolution mechanism for cross-axis signal-set integration; per-axis observability hook signal-set Phase 6 spec target resolves E2.
- **Resolves `arch/scope-model.md` §18 per-primitive composition table quality-gate row** — gate Implementation = Framework C distributable per shape; gate Running Instance = Owner B workspace-bound.
- **Reciprocal composition with cross-axis category-collapse counter-mechanism** — quality-gate is the architectural counter-mechanism for category-collapse per `arch/axis-interactions.md` §4.3; CC-1 cross-axis failure cascade pattern per `arch/axis-interactions.md` §3.4 is the structural reason quality-gate Surface is single-layer cumulative observability.
- **Composes with all primitives in §7** — substrate (observability infrastructure + boot precondition) / adapter (orthogonal mechanism category — gate fires at adapter-bounded actions) / sparring (axis-2 observability source) / audit (gate emits via Surface §A; gate state-restore reads via Surface §C) / specialist-skill (per-skill gate-firing requirements per E8) / practitioner (HITL approval moments via substrate Surface §C) / workflow-work-unit (workflow_instance + work-unit observability sources) / claim-defensibility (per-claim attestation chain IS gate's axis-3 observability source) / scope-model (cross-cutting integrator scope-categorization composition) / axis-interactions (cross-axis category-collapse counter-mechanism).

## 8. Constraints flowing to downstream commitments

This decision flows constraints into:

- **Phase 6 Pydantic Protocol contract** — Mode 3 spec lands typed Pydantic Protocol (`docs/specs/quality-gate.md`); GateError class hierarchy; supporting Pydantic types (CheckpointKind / InterventionKind / GateState / etc.)
- **Phase 6 per-impl spec** — practitioner-shape-gate full impl + autonomous-business-shape-gate full impl + personal-OS-shape-gate full impl + research-lab-shape-gate at second-shape productization per W1
- **Phase 6 per-axis observability hook signal-set spec** — resolves `arch/axis-interactions.md` §7 E2 Phase 6 spec target; per-axis signal-set + threshold semantics + diagnostic emission contract
- **Phase 6 cross-axis error catalog spec** — resolves `arch/axis-interactions.md` §7 E4 Phase 6 spec target; cross-axis error catalog + recovery semantics + per-shape fail-closed vs fail-open declaration
- **Phase 6 specialist DEFINITION manifest schema `gate_firing_requirements` field** — per E8 specialist per-skill gate-firing; manifest schema field `gate_firing_requirements: optional` Phase 6 spec target; composes through specialist-skill primitive-cluster topic per `arch/specialist-skill.md`
- **Phase 6 per-impl extension Protocols pattern** — practitioner-shape-gate extension exposes practitioner-attestation-extension; autonomous-business-shape-gate extension exposes budget-policy-extension; personal-OS-shape-gate extension exposes drift-check reporting extension
- **Phase 1+ pioneer per-shape threshold tuning** — per W2 watch-list awaits pioneer deployment data; per-axis signal-set + threshold-set evidence accumulation
- **Second-shape productization concrete impls** — per W1 watch-list awaits second-shape design begin signal; cross-link to BACKLOG shape-neutrality watch-list + `arch/scope-model.md` W4 + `arch/axis-interactions.md` §14 W1
- **`ARCHITECTURE.md` §6 amendment** — composite boot+shutdown sequence amendment for gate-phase 1-4 (REQUIRED in Wave-2 cascade-pass per cascade discipline)

## 9. Files touched

- `arch/quality-gate.md` (NEW; Pattern A 12+7 third instance; FORMAL STABILITY achieved with 3 of 3 Pattern A instances complete; 17 sections — 12 common + 4 conditional applies §10+§11+§13+§14 + 3 N/A documented §3+§8+§12)
- `docs/decisions/quality-gate-arch-topic.md` (NEW; this DR; composite Mode 2 upfront-known decomposition; 6 sub-decisions)

Wave-2 cascade follow-ups (downstream cascade per cascade discipline): `ARCHITECTURE.md` §2 sub-phase status (3.6 LOCKED) + §4 topic catalog (quality-gate DRAFTED → LOCKED) + §6 cross-cutting principles "Workspace boot + shutdown composite sequence" subsection (gate-phase 1-4 amendment) + §7 locked architectural decisions (quality-gate ARCH topic Phase 3.6 LOCKED entry); `GLOSSARY.md` quality-gate index entry (placeholder forthcoming → LOCKED link); `glossary/quality-gate.md` "See" section (placeholder forthcoming → LOCKED link); `BACKLOG.md` Phase 3.6 entry (1 of 1 ARCH topic LOCKED); `HANDOFF.md` (Phase 3.6 close note); cross-corpus sweep per cascade discipline mandatory cross-corpus sweep at Phase 3.6 phase-set closure (placeholder framings of OTHER terms now made citable by Phase 3.6 close).

### Wave-2 cascade applied (commit `<Wave-2-hash>`)

Wave-2 cascade applied 2026-05-04 across the following files (~30+ file edits — Wave-2-hash placeholder pending Wave-2.5 Cleanup-Writer resolution):

- **ARCHITECTURE.md** — §2 row 3.6 CLOSED + §3 11/11 LOCKED + §4 row 5 DRAFTED + §6 composite boot+shutdown sequence gate-phase 1-4 + gate-shutdown 1-4 amendment + §7 NEW lock entry quality-gate Pattern A 12+7 third instance with FORMAL STABILITY codification
- **MAINTENANCE.md** — Pattern A protocol topic template subsection: FORMAL STABILITY language addition (3 of 3 Pattern A instances complete) + per-topic section count expectation quality-gate row update (19 numbered §-headers + 4 conditional applies + 3 N/A documented)
- **BACKLOG.md** — Phase 3.6 row resolved cluster format + 3-tier discriminator cumulative-count update 8 → 10 across 7 cluster-executions (preserves freeze 2026-05-04 + signal-(a) DEFER WITH NAMED AWAIT-SIGNAL per Note 62) + Phase 3.7 + Phase 3.8 unblocked notation
- **glossary/quality-gate.md** — See section: placeholder until Phase 3.6 → LOCKED link to `arch/quality-gate.md`
- **10 peer ARCH topics §17/§19 reciprocal back-mentions upgraded to LOCKED**: substrate + adapter + sparring + audit + specialist-skill + practitioner + workflow-work-unit + claim-defensibility + scope-model + axis-interactions (axis-interactions §7 E2 + §14 W4 RESOLVED notation; cross-corpus sweep ~15 forward-references upgraded)
- **Cross-corpus sweep per `MAINTENANCE.md` Cascade 1 commit `f075927` structural amendment** — 7 glossary entries upgraded (category-collapse + answer-machine-ai + oracle-ai + validator-ai + rubber-stamping + workflow + protocol-architectural); all "Phase 3.6 forthcoming" / "(forthcoming)" / "placeholder until Phase 3.6" framings of quality-gate-related references upgraded to LOCKED links

Wave-2 commit hash placeholder `<Wave-2-hash>` to be resolved by Wave-2.5 Cleanup-Writer post-Wave-2 commit landing.

## 10. Revisit triggers

This DR should be revisited if:

- **Phase 6 surfaces operational concerns not anticipated at ARCH topic time** — per scope-lock DR Revisit triggers carried forward; Phase 6 Pydantic Protocol contract + per-impl spec + per-axis observability hook signal-set spec may surface schema-detail concerns requiring §15 forward-reference revision
- **Second-shape productization reveals Surface needs adjustment** — per W1 watch-list resolution; second-shape gate-impl may surface Surface contract gaps (currently 6 capability categories validated against 3 concrete impls + 1 preliminary research-lab; second-shape may surface additional categories or refine existing categories)
- **Pioneer-deployment data shows Pattern A pluggability over- or under-specified** — per W2 + W4 watch-lists; per-axis signal-set + threshold-set evidence may surface Pattern A pluggability gaps or over-specification
- **New named Protocol surfaces subsuming gate's role** — unlikely per scope-lock DR Revisit triggers; would require novel cross-axis category-collapse counter-mechanism architectural pattern
- **Cross-deployment gate-state portability evidence surfaces audit-trail composition gaps** — per W3 watch-list resolution; first cross-deployment claim ingestion deployment may surface audit-trail-as-state-store reframe (E2) gaps
- **VISION axis-3 falsification signal accumulates** — per `VISION.md` §Falsification criteria; real defensibility events where audit-trail provided no benefit would trigger gate's axis-3 intervention efficacy revision per W4
- **Pattern A topic-template-class extension trigger** — per per-pattern instance-driven trigger pattern; if 4th Pattern A instance surfaces (currently complete with 3 of 3) and surfaces 8th conditional candidate, template extension would trigger this DR + sibling Pattern A DR amendments per cascade discipline
