"""Scanner / OCR integration adapters.

Available adapters (by `office_config.integrations.scanner.adapter`):
- `none` (default)
- `hot-folder` (planned — watch a folder for new scans)
- `escli` (planned — direct scanner control)
- `tesseract` (planned — OCR existing PDFs)
"""
