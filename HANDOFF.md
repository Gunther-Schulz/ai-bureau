# Session handoff — pbs-bureau

End of session 8 (2026-04-29). This session executed pre-RAG
commitment **#10 (A2A schema compatibility + Gemini Enterprise
pattern emulation)**, the smallest of the 13 pre-RAG commitments
and the one informing every downstream schema decision. Pulled to
position 1 at end of session 7; landed cleanly in one session as
planned.

**What shipped**:

- **Decision record**: `docs/decisions/a2a-and-gemini-pattern-
  emulation.md` — per-row decisions across the v2 Gemini
  Enterprise comparison table (orchestrator / backend transport /
  persistent state / cross-department workflow / audit trail /
  governance / model). Both defensive (A — A2A-shape
  compatibility) and proactive (B — pattern emulation) sides
  covered with verdict + rationale per row.
- **AuditEvent schema additions** (`backend/mcp-server/src/pbs_mcp/audit_trail.py`):
  - `ActorKind = Literal["human", "skill", "external_agent"]`
  - `actor_kind: ActorKind` — required (per strict-validation
    discipline; no silent default)
  - `actor_card: str | None = None` — optional, reserved for
    agent-card URL (A2A future)
  - `origin_agent_card: str | None = None` — optional, source A2A
    peer when external_agent
  - `_check_external_agent_origin` model validator: external_agent
    events MUST name origin_agent_card
- **ARCHITECTURE.md v0.9 → v0.10**: changelog entry summarizing
  the per-row decisions; meta-rule 3 invalidation table
  (Memory/audit-log row) updated to reflect new fields + v2 single-
  write architecture.
- **ROADMAP.md commitment #10**: collapsed to shipped-summary;
  full reasoning lives in the decision record.

**Constraints recorded for downstream commitments** (the
load-bearing output of #10 — these constrain how #6, #11, #12,
#13 implement):

- **#6 (audit-trail v2 retrofit)**: every `record_audit_event` /
  `record_decision` call MUST set `actor_kind` explicitly. Skill
  retrofits pass `actor_kind="skill", actor_card=<skill-name>`.
  `user_confirmation` events emit as `actor_kind="human"`.
- **#11 (Cowork integration)**: plugin agents emit as
  `actor_kind="skill", actor_card=<agent-name>`.
- **#12 (office-vs-department modularization)**: cross-department
  coordination MUST be event-shaped (typed events on audit trail
  or sibling mechanism), not call-shaped. Preserves transport-
  swap-to-A2A path. Department identity is independent of
  actor_kind — a department is a grouping of skills, not an
  actor kind.
- **#13 (deployment flexibility)**: HTTP MCP transport
  implementation, data classification annotations, and
  AuditEvent.user_id (multi-user readiness) all land in #13. #10
  decided HTTP MCP adoption; #13 builds it.

**Deferred to Tier 3 migration trigger**:
- Cryptographic signing fields on AuditEvent
- Deterministic JSON canonicalization function
- Memory Bank persistent-context port
- Apigee MCP-to-agent bridge

**Deferred with revisit triggers documented**:
- Session-spanning "office-memory" concept (revisit if cross-
  project scope-spanning state emerges)
- Model Armor analogue (revisit at #13 HTTP MCP transport)
- Agent Simulation analogue (revisit in v2)

**Smoke-test results** (no test infrastructure yet — five-case
inline `uv run python` verification):
- Skill-emitted event with actor_card: ✅
- Human event without actor_card: ✅
- external_agent without origin_agent_card: ❌ (correctly raises)
- external_agent with origin_agent_card: ✅
- Missing actor_kind: ❌ (correctly raises ValidationError)

---

## Read order for next session

1. **This file (HANDOFF.md)** — current state
2. **`ARCHITECTURE.md`** — **v0.10**. Pattern-vs-instance discipline
   intact; meta-rule 3 invalidation table reflects AuditEvent's
   new fields + v2 single-write.
3. **`docs/decisions/a2a-and-gemini-pattern-emulation.md`** — the
   load-bearing artifact of session 8. Per-row verdicts +
   constraints for #6/#11/#12/#13. Read in full before tackling
   the next commitment.
4. **`docs/decisions/`** — other authoritative records:
   - `mcp-fallback-policy.md` (session 7, fail-closed corollary)
   - `trigger-convention.md` (session 7, concept labels)
   - `audit-trail-v2.md` (session 7, single-write supersedes v1)
   - `audit-trail-v1.md` (SUPERSEDED, header note)
   - `sparring-output-v1.md` (session 6)
   - `backend-{test-layout,logging,mcp-error-format}.md` (session 5)
5. **`ROADMAP.md`** — commitment #10 marked shipped; remaining
   pre-RAG queue is **#12 → #11 → #13 → #6 → #7 → #9 → #8 → C → D**.
   Generalize-and-publish in v1.x. AI-office builder in v2.
6. **`docs/plugin-conventions.md`** — §11 (triggers) + §11b
   (fail-closed fallback policy)
7. **`VISION.md`** — pioneer-instance milestones; "First office
   generated by AI-office builder" extended end-state
8. **`docs/audits/`** + **`docs/design-reviews/`** — session-6
   first runs + session-5 foundations
9. **`docs/rag-pipeline-decisions.md`** — Phase 0/1/2/3/4 phasing
10. **`docs/backend-conventions.md`** — backend idioms
11. **`plugin/CLAUDE.md`** — meta-rule 4 summary; pattern-vs-
    instance discipline pointer
12. **`plugin/skills/audit/`** — **0.8.0** (slices 1-16 + 18 + 19)
13. **`plugin/skills/design-review/`** — **0.7.0** (targets 1-10)
14. **`plugin/skills/orchestrator/`** — **0.10.0**
15. **`backend/mcp-server/src/pbs_mcp/audit_trail.py`** — updated
    schema with ActorKind + new fields + cross-ref validator
16. All other 16 skills — per session 7 versions snapshot

---

## ⏳ Pre-RAG gating items (12 commitments remaining, post-session-8)

The 13 v1 commitments enumerated in ROADMAP.md "v1 commitments"
section. **#10 ✅ shipped session 8.** Recommended execution order:

```
Session 9:    #12 (department modularization design)          1 session
Session 10-13: #11 (deep Cowork integration refactor)         3-5 sessions
Session 14-16: #13 (deployment flexibility + Coolify ref dep)  2-3 sessions
Session 17+:  #6 → #7 → #9 → #8                               per existing queue
              C (sparring-output integration)
              D (plugin version bump)
Then:         Phase 0 items 4 + 5 → Phase 1 corpus
```

### Already shipped (architectural backstops)

1. ✅ **Unified audit trail v1** — schema + Pydantic + 2 MCP
   tools shipped session 6.
2. ✅ **Sparring-output structural promotion** — schemas + MCP
   tool + plugin-conventions field shipped session 6.
3. ✅ **State.md MCP gate** — Pydantic + 2 MCP tools shipped
   session 6. **Skill retrofits done session 7.**
4. ✅ **Fail-closed corollary** — done session 7 (decision record
   + meta-rule 4 corollary + slice 14 brief + 19-skill sweep).
5. ✅ **Trigger-convention simplification** — done session 7.
6. ✅ **Audit-trail v2 decision** — record done session 7;
   implementation deferred to commitment #6 (in remaining queue).
7. ✅ **A2A schema compatibility + Gemini Enterprise pattern
   emulation** — **done session 8** (this commitment). Decision
   record + AuditEvent schema additions + ARCHITECTURE bump.

### Remaining for next-immediate-session-before-RAG

**#12 — Office-vs-department modularization design** (ROADMAP
commitment #12) — **POSITION 1 in remaining queue**:
- Architectural insight: PBS today conflates "office" with
  "single department" (planning-document-work). Real offices
  contain multiple departments (planning + PM + invoicing).
- Outputs (1 session, primarily design):
  - `docs/decisions/office-vs-department.md` — architectural
    distinction + chosen implementation approach
  - Skill frontmatter convention: `department: <slug>` field
  - Office-config schema: `departments.<name>` sections
  - Memory taxonomy decision: department axis vs domain forking
  - Cross-department workflow patterns documented
- **#10 constraint**: cross-department coordination MUST be
  event-shaped, not call-shaped. Coordinate via typed events
  (likely AuditEvent kinds) so transport-swap-to-A2A is later
  available without refactor.
- Method: classify current PBS skills as office-level
  scaffolding vs department-of-planning-work; design boundary;
  specify PM + invoicing slot-in.
- Scope: design + decision record, not heavy implementation.

**#11 — Cowork as primary end-user runtime, DEEP integration**
(ROADMAP commitment #11) — **POSITION 2 in remaining queue**:
- Cowork natively supports MCP + Skills + Plugins. Anthropic's
  open-source `knowledge-work-plugins` repo (cloned to
  `~/dev/reference/knowledge-work-plugins/`) is the reference
  pattern.
- "Deep + complete integration, no sunk costs" directive: adopt
  Anthropic's plugin shape wholesale where it differs.
- Concrete work items (3-5 sessions):
  - Plugin shape: `.mcp.json`, `marketplace.json`,
    `CONNECTORS.md`, polished README
  - Slash commands as primary user surface, department-namespaced
    per #12 (`/planning:draft-begruendung`, etc.)
  - Plugin agents formalized: `research-references-fetcher`,
    `audit-slice-runner`, `design-review-target-runner`. Per
    #10 constraint: emit events with `actor_kind="skill",
    actor_card=<agent-name>`.
  - Migrate `office-config.yaml` → `pbs.local.md`
  - Skill frontmatter alignment + `department:` field
  - Test in actual Cowork (not just Claude Code) — end-to-end
    deploy + workflow test
- Output: `docs/decisions/cowork-deployment.md` + extensive
  refactor work touching every user-facing surface.

**#13 — Deployment flexibility + Coolify reference deployment**
(ROADMAP commitment #13) — **POSITION 3 in remaining queue**:
- Tier 1 (local) ↔ Tier 2 (cloud-hosted container) = same
  archetype, different transport/persistence/auth. Pluggable
  layers.
- Tier 3 (Gemini Enterprise) is intentionally NOT in this
  commitment — separate ROADMAP v2 entry; activates only if
  enterprise scale arises.
- Per #10 constraint: HTTP MCP transport implementation lands
  here. AuditEvent.user_id field for multi-user attribution
  lands here. Data classification annotations land here.
- Output: pluggable persistence + auth + transport
  abstractions; decision records (deployment-mode-flexibility,
  user-identity-and-multi-user); reference Coolify deployment
  for Schulz Planungsbüro with two-user end-to-end test.
- Scope: 2-3 sessions.

**#6 — Audit-trail v2 retrofit** (per `audit-trail-v2.md`):
- Backend: build `record_decision` (atomic dual-write inside
  gate); build `render_audit_trail` (deterministic prose
  synthesis). Add `user_confirmation` event kind; expand
  `decision`/`module_decision` `details` to require
  `reasoning_full_text`. Drop `phase_history` from ProjectState;
  write migration script.
- **Per #10 constraint**: every retrofit explicitly sets
  `actor_kind` (skill / human / external_agent) on every event.
  No defaults; strict-validation discipline.
- Skills: orchestrator + save-baustein + record-feedback +
  draft-textteil-b/c + review-draft + research-references
  declare `record_audit_event` (and `record_decision` where
  applicable) in `mcp_tools_required` + invoke at appropriate
  checkpoints.
- Migration: `backfill_audit_trail` walks legacy prose sources,
  emits events, optionally retires subsumed source files after
  round-trip render comparison.

**#7 — Bootstrap-write MCP tools** (ROADMAP commitment #7):
- Build `create_manifest` + `create_office_config` (Pydantic-
  validated first-write through loader; replace direct Write).
- Skill retrofits: `author-manifest` + `setup-office` declare
  the new tools + use them in body.
- Drop the "known bypass" caveat from fallback strings.
- Audit slice 14 re-run after retrofit.

**#9 — Pattern-vs-instance best-effort split** (ROADMAP
commitment #9):
- Reasoning + implementation + PBS regression validation,
  bundled. 1-2 sessions; could be small (semantic) or large
  (structural) refactor.
- Targets: ProjectState core/extension split; office-config
  core/extension split; doctype-manifest schema generalization;
  review MCP tool interfaces for PBS-coupling.
- Output: refactored Pydantic + skill retrofits + passing PBS
  tests + `pattern-vs-instance-split-rationale.md` documenting
  per-commitment reasoning.
- Order note: schedule AFTER #6 + #7 (so #9 examines stable
  post-v2 schemas + bootstrap-write tool interfaces); BEFORE #8.

**#8 — Pre-action framing skill** (ROADMAP commitment #8):
- Design + scaffold a new meta-skill (working name `frame-task`
  or `scoping`).
- Triggered on non-trivial task starts; outputs a brief framing
  artifact (problem, scope, approach, constraints, success-
  criteria) that implementation skills consume.
- **Order note**: schedule AFTER #9 — codifies pattern-vs-
  instance reasoning produced by #9 into a repeatable check.
- Decision: build skill bundle (SKILL.md + references/); decide
  if it warrants a PROCEDURE.md (probably yes — phase gates).

### Sparring-output integration (still per v1 plan)

- `review-draft` declares `output_schema: ReviewOutput` in
  frontmatter; body adds Output Format section.
- `orchestrator` PROCEDURE.md Checkpoint 13: declare phase-
  specific `RecommendationOutput` schema; call
  `validate_skill_output` post-output; loop on missing-fields
  up to 3x.
- Heuristic markdown-field parser refinement after first
  real-use feedback.

### Plugin version bump

- `plugin.json` 0.3.0 → 0.5.0 after #6/#7/#9 retrofits land.
  Run `bash dev-link.sh` after.

### Then — Phase 0 items 4 + 5

After all v1 commitments land:
- **Phase 0 item 4 — Feature-survey skill**: greenfield-the-
  vision sibling to audit + design-review.
- **Phase 0 item 5 — Testing methodology + harness**: discussion-
  first. `docs/rag-testing-strategy.md` output.

### Then — Phase 1 corpus download

Fetch all 57 entries via `research-references` full refresh.
**No embeddings yet** — raw fetch + checksum + manifest population.

---

## Key paths reference

| Path | Purpose |
|---|---|
| `/home/g/dev/Gunther-Schulz/pbs-bureau/` | This repo |
| `VISION.md` | Three-axis thesis (canonical "why") |
| `ARCHITECTURE.md` | **v0.10** + A2A-shape commitment + AuditEvent actor_kind |
| `ROADMAP.md` | 13 pre-RAG commitments (#10 ✅ shipped); generalize-publish in v1.x; AI-office builder in v2 |
| `~/dev/reference/knowledge-work-plugins/` | Cloned Anthropic plugins repo for #11 study |
| `docs/decisions/a2a-and-gemini-pattern-emulation.md` | **Session-8 deliverable** — per-row decisions + downstream constraints |
| `docs/decisions/mcp-fallback-policy.md` | Session-7 fail-closed corollary |
| `docs/decisions/trigger-convention.md` | Session-7 concept labels |
| `docs/decisions/audit-trail-v2.md` | Session-7 reversal; supersedes v1 |
| `docs/decisions/audit-trail-v1.md` | SUPERSEDED |
| `docs/decisions/sparring-output-v1.md` | Session-6 v1 commitment |
| `docs/decisions/backend-{test-layout,logging,mcp-error-format}.md` | Session-5 backend records |
| `docs/audits/boundary-adherence-20260429.md` | Slice 14 first run (session 6) |
| `docs/audits/invalidation-contract-20260429.md` | Slice 15 first run (session 6) |
| `docs/audits/validation-gate-20260429.md` | Slice 16 first run (session 6) |
| `docs/design-reviews/vision-arch-coupling-20260429.md` | Target 8 first run (session 6) |
| `docs/design-reviews/foundations-20260429.md` | Session-5 design-review |
| `backend/mcp-server/src/pbs_mcp/audit_trail.py` | **Updated session 8** — ActorKind + 3 new fields + cross-ref validator |
| `plugin/skills/audit/` | **0.8.0** — slices 1-16 + 18 + 19 |
| `plugin/skills/design-review/` | **0.7.0** — targets 1-10 |
| `plugin/skills/orchestrator/` | **0.10.0** |
| `plugin/CLAUDE.md` | Updated meta-rule 4 summary (fail-closed line) |
| `docs/plugin-conventions.md` | §11 (triggers) + §11b (fallback policy) |
| `~/.config/pbs-bureau/office.yaml` | v3 (session 6 migration) |

---

## Skill versions snapshot (post-session 8 — unchanged from session 7)

No skill versions changed this session. Session 8 was backend-
schema + decision-record work; no skill bodies touched. The
session-7 versions snapshot remains current. (Will change next
session when #12 introduces `department:` frontmatter field.)

| Skill | Version |
|---|---|
| audit | 0.8.0 |
| author-manifest | 0.4.0 |
| design-review | 0.7.0 |
| draft-cover-mail | 0.6.0 |
| draft-textteil-b | 0.5.0 |
| draft-textteil-c | 0.5.0 |
| orchestrator | 0.10.0 |
| promote-to-skill | 0.5.0 |
| record-feedback | 0.4.0 |
| research-references | 0.5.0 |
| review-draft | 0.5.0 |
| save-baustein | 0.4.0 |
| setup-office | 0.6.0 |
| survey-project | 0.5.0 |
| validate-bausteine | 0.4.0 |
| validate-checklist | 0.6.0 |
| validate-latex-style | 0.5.0 |
| verify-citations | 0.5.0 |
| watch-list | 0.2.0 |
| plugin.json | 0.3.0 (will bump to 0.5.0 after #6/#7/#9) |

---

## MCP tools shipped session 8

None this session — schema additions to existing AuditEvent only.
The session-6 tools (5 new) remain the current backend surface.
Backend tools planned for next-immediate-session retrofit:
- `record_decision` (audit-trail v2 — for #6)
- `render_audit_trail` (audit-trail v2 — for #6)
- `create_manifest` (bootstrap-write — for #7)
- `create_office_config` (bootstrap-write — for #7)

Schema-side, AuditEvent gained `actor_kind` (required) +
`actor_card` + `origin_agent_card` (both optional). Validator:
`external_agent` requires `origin_agent_card`.

---

## Working-style notes (carried + new)

1. **Pre-action framing matters more than post-action review** —
   carried from session 7. Today the audit-trail v1→v2 reversal's
   subsumption lesson held: session 8's #10 work explicitly asked
   "what does this subsume?" (5 deferred items + Tier 3 migration
   path). No legacy left in place.

2. **Defensive pre-RAG schema additions are nearly free** —
   confirmed session 8. Three optional/required fields on
   AuditEvent took ~20 minutes of edit + smoke-test work.
   Migration cost post-data-accumulation would be a multi-skill
   retrofit + schema migration. Asymmetric ratio argues for over-
   shooting on cheap defensive additions.

3. **"No menus, commit to positions"** (carried from
   feedback_judgment_and_automate). Session 8 produced verdicts
   per row of the comparison table; the decision record argues
   each verdict but doesn't present the reader with a choice.

4. **Pattern-vs-instance discipline survives archetype questions**.
   The pattern-vs-instance test (cross-domain portability) and
   archetype-portability question (single-Opus vs multi-agent
   A2A) are orthogonal. Session 8 kept them separate in
   ARCHITECTURE.md — the discipline section unchanged; the
   archetype-portability rationale lives in the decision record
   + v0.10 changelog. Conflating them would muddy both.

5. **Test infrastructure deferred is technical debt**. Session 8
   smoke-tested via inline `uv run python` because no backend
   test directory exists. This is acceptable for #10's small
   surface (3 fields + 1 validator) but won't scale through #6/
   #7/#9. Establishing the test pattern is its own work item —
   probably folded into #6's audit-trail v2 retrofit.

6. **Memory captures**: existing 6 feedback memories carry
   forward. The "leave legacy behind" + "judgment-not-menus"
   principles paid off this session.

---

## Session 8 commits (chronological)

| # | Commit | Theme |
|---|---|---|
| 1 | (this commit) | session 8: pre-RAG #10 shipped — A2A schema compatibility + Gemini Enterprise pattern emulation. Decision record + AuditEvent schema additions (ActorKind, actor_kind required, actor_card + origin_agent_card optional, cross-ref validator) + ARCHITECTURE v0.9→v0.10 + ROADMAP/HANDOFF refresh. |

Pushed to origin/main.

---

## Misc context for next session

- **User's machine**: Linux, RTX 5090 (32GB VRAM). Python 3.13.
- **Plugin cache symlink**: bump `plugin.json` AND re-run
  `bash dev-link.sh` after #6/#7/#9 retrofits.
- **Hooks active**: `restrict-bash-paths.py`,
  `restrict-file-paths.py` in dotfiles. Hidrive path whitelisted.
- **Settings symlink**: verify
  `~/.claude/settings.json -> dotfiles/claude/settings.json`
  before any operation that might write settings.
- **Office-config**: v3 on disk; no `path_classification` block.
- **No projects bound yet**: AuditEvent retrofits are design-
  time-pending until first project bind. Backfill is academic
  until first-bind.
- **Auto-memory** at `~/.claude/projects/.../memory/`:
  - `feedback_blocked_actions.md`
  - `feedback_judgment_and_automate.md`
  - `feedback_push_after_commit.md`
  - `feedback_refine_pareto.md`
  - `feedback_defer_instinct.md`
  - `feedback_llm_instruction_tightness.md`
