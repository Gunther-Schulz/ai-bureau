# Deployment Inventory (outside this repo)

> Scope: artifacts OUTSIDE `/home/g/dev/Gunther-Schulz/pbs-bureau/`. The framework-side inventory is a sibling document.
> Purpose: enumerate what's already deployed/in-use, so the Cowork build (per `5-PIVOT-DECISION.md`) knows what to wrap, replace, absorb, or treat as gap.

## Summary

**Current deployment state**: Two distinct deployment artifacts coexist. (1) `pbs:* plugin v0.1.0` is the **actually-used production deployment** — 14 Claude Code skills loaded via the plugin cache, currently running Gunther's daily planning-office work (orchestrator + 13 specialists, all v0.1.0, German B-Plan / Festsetzungen / Umweltbericht workflows). The plugin description references an MCP backend (LanceDB + fastembed + LaTeX compile) which is **NOT YET BUILT** — every skill currently falls back to `Read`/`Glob`/`Grep`/`Bash`. (2) `pbs-dep-1/` is an M1 "Hello PBS" proof-of-life harness for the (now-paused) framework Python runtime; it boots the framework's substrate + audit + authority-binding mechanisms end-to-end with a stub `say-hello` skill. It runs cleanly but is not connected to real client work.

**pbs-dep-1**: minimal, working, framework-internal. Audit trail persists; substrate boots; `claim_made` event flows. Surfaced 3 API-friction findings (severity workaround-acceptable). Per pivot decision §7 this is "historical artifact; M1 Hello PBS proof-of-life."

**pbs:* plugin (frozen 0.1.0)**: 14 skills, ~1700 lines of SKILL.md content total, deeply embeds PBS-domain workflow + orchestrator gates (compile / layered review / send / state transition). PROCEDURE.md files extend orchestrator + draft-textteil-b + review-draft. **This IS the planning-office workflow as it lives today** — independent of any framework Python runtime.

**Planungsbüro Schulz workspace**: 18 client projects (`YY-NN Client - Location` pattern, 2022–2025) at `/mnt/data2t/hidrive/Öffentlich Planungsbüro Schulz/Projekte/`, plus shared `_ai-office-state/` (projects-index, pending-actions, recent-correspondence, office-identity templates) and `_ai-references/` (gesetze/{bund,eu,MV}, leitfaeden, urteile, beispiele directories). The references corpus directories exist but are **largely empty** — gesetze stubs only, leitfaeden/urteile/beispiele empty. The intended-but-not-built RAG corpus.

**Top-3 things already-working that the Cowork build must not regress**:
1. The **orchestrator gate discipline** (compile gate / layered review gate / send gate / state-transition gate) — this is the PBS-specific operational shape the user works in daily; any rewrite must preserve gate semantics.
2. The **four-way decision menu** (capture-now / handle-now / backlog / drop) + watch-list triggers (T1–T6) — the operational interaction shape the user is trained on.
3. The **canonical project layout + project-binding flow** — `YY-NN Client - Location` naming, `_ai/` per-project state, `survey-project` first-bind clustering. Substantial workflow muscle memory.

**Top-3 gaps the Cowork build must close**:
1. **The MCP backend never got built** — all `search_corpus / read_corpus_file / list_bausteine / compile_latex / scaffold_project` calls fall back to native tools today. This is the single biggest delta between what skills assume and what runs. Cowork build must materialize as actual MCP servers.
2. **Reference corpus is empty** — `_ai-references/` directories exist but have ~no content. The Bauleitplanung-specific corpus (BauGB / BNatSchG / DE-MV regional + leitfäden + urteile + beispiele) is a `research-references` skill responsibility but has nothing populated. Cowork build must seed this.
3. **No persistent workspace state across sessions** — `pbs-dep-1` was the framework-side approach; with framework paused, equivalent persistence on Cowork-native primitives is undefined.

---

## By artifact

### A. pbs-dep-1 repo

Path: `/home/g/dev/Gunther-Schulz/pbs-dep-1/`. ~~140 LOC across 2 Python modules + 2 markdown findings.

**README.md** characterizes this as "Track 2 of the lock-and-park pivot... where evidence is generated by actually deploying and running PBS." Per §7 of the pivot doc this repo is now reclassified as **historical artifact**.

**pyproject.toml**: Python 3.11+; depends on `pbs` framework via path-editable (`../pbs-bureau`). Strict mypy. Uses hatchling.

**runtime/**:
- `hello_pbs.py` (225 LOC) — boots framework end-to-end: instantiates `PractitionerShapeAuthorityBinding` → boots `ClaudeAgentSDKAudit` (jsonl-backed audit trail) → boots `ClaudeAgentSDKSubstrate` → registers `hello-specialist` SpecialistDescriptor → emits synthetic `claim_made` event → clean shutdown. Verifies audit-trail file is non-empty.
- Surfaces exercised: Substrate Surface §G specialist registration, Audit Surface §A event emission, authority-binding gate, hash-chain audit-trail integrity, lifecycle.
- Deferred (M2+): real claim attestation (claim_attested needs HUMAN actor), sparring sub-mechanism, quality-gate, real Claude SDK agent invocation (substrate.run_agent raises NotImplementedError).

**runtime-state/**:
- `hello-pbs-audit.jsonl` — persistent audit trail. Existence and non-empty count is the M1 success signal.

**workspace/**:
- `workspace.md` — manifest with `workspace_id: pbs-dep-1-hello`, `shape: practitioner-shape`, `substrate: claude_agent_sdk`, single `hello-specialist@0.0.1`, single `test-practitioner`. Placeholder `legal_entity_context` ("PBS Test Deployment Entity / TEST / TEST-XX"). Empty `adapter_bindings`, empty `scope.domains`, empty `scope.states`. Comments explicitly note "real PBS-Schulz GmbH context lands at M2."
- `practitioners/test-practitioner.md` — placeholder.
- `specialists/hello-specialist/` — placeholder.

**findings/**:
- `2026-05-06-m1-api-friction.md` — three API-shape findings (severity workaround-acceptable): (1) `AuditShapePolicy` exported from impl module not Surface, (2) `is_ready` vs `audit_storage_ready` naming inconsistency, (3) sync `emit()` vs general async pattern. None block; all signal API cleanup needed before more deployers come in.
- `TEMPLATE.md` — finding-author template.

**Classification**: **drop** (per pivot). Repo serves as preserved decision-trail evidence that the framework boots; nothing here translates to Cowork-native work. The audit-trail JSONL idea may inform Cowork event-log design but the Python harness itself is dead-end.

---

### B. pbs:* plugin (frozen 0.1.0 cache)

Path: `/home/g/.claude/plugins/cache/pbs-bureau/pbs/0.1.0/`. Plugin manifest declares: "Planungsbüro Schulz document workflow assistant. Master orchestrator coordinates drafting, review, and finalization of B-Plan, Festsetzungen, Umweltberichte, and Gutachten using a local in-process RAG backend (LanceDB + fastembed) and per-project memory in _ai/."

`CLAUDE.md` system map: Plugin (this dir) + Backend (`<repo>/backend/`, **NOT BUILT**) + Cross-cutting memory (`<repo>/memory/`) + Per-project memory (`<project>/_ai/`).

Note: This plugin's `<repo>` reference is the conceptual "pbs-bureau plugin repo" described in CLAUDE.md, not the framework source repo of the same name. The two share a name historically but the plugin assumes a different on-disk layout (with `backend/` + `memory/` siblings to `plugin/`) that does not exist in the current framework source repo. Cowork build must clarify this.

**14 skills** (line counts in parens):

| Skill | Lines | Purpose (1-line) |
|---|---|---|
| `orchestrator` | 96 + PROCEDURE 425 + references/state-format 290 | Master skill; auto-loads on PBS context; coordinates drafting/review/finalize across all doctypes; runs gates, watch-list, source-grounding guard, lifecycle, ownership modes, project binding, MCP routing. |
| `draft-textteil-b` | 129 + PROCEDURE | Phase A entry: drafts B-Plan Begründung (Textteil B) section-by-section from project sources, applying bausteine + corpus refs. |
| `draft-textteil-c` | 137 | Phase A entry: drafts B-Plan Textliche Festsetzungen (Satzung Teil B Text). |
| `review-draft` | 134 + PROCEDURE | Phase B entry: layered review (structural → fachlich → formal) with per-layer pause + delegation to validate-checklist + validate-latex-style + verify-citations. |
| `validate-checklist` | 123 | Layer 1 of layered review: doctype-specific required-sections + macros + end-blocks check. |
| `validate-latex-style` | 123 | Layer 3 of layered review: diff document preamble vs canonical `style-spec.md`. |
| `verify-citations` | 116 | Cross-checks legal citations (§-paragraphs, BVerwG rulings, Verordnungen) against reference index. |
| `validate-bausteine` | 130 | Periodic freshness sweep: review_due dates / status=flagged / citation-drift / successive UNB rejections. |
| `save-baustein` | 98 | Capture reusable text/argument/spec/citation to memory at right scope (global/domain/project). |
| `promote-to-skill` | 114 | Memory→skill graduation when baustein reaches ≥3 use references; guarded by source-project finalized state. |
| `record-feedback` | 106 | Capture external feedback (UNB rejection / approval / partial / suggestion) on PBS work. |
| `survey-project` | 156 | First-bind file-clustering for existing projects (artifacts / inputs / sent / correspondence / cruft). |
| `research-references` | 126 | Maintain reference corpus: fetch / update / freshness-check gesetze + leitfäden + urteile per `references-manifest.yaml`. |
| `draft-cover-mail` | 118 | Draft transmittal cover mail (Anschreiben) to authorities; part of send gate. |

**Notable: orchestrator structure** (per `PROCEDURE.md` 425 LOC):
- Checkpoint 1: Session open — detects scope (office / project / product), loads state, announces in one terse line.
- Checkpoint 2: Continuous watch list — six triggers (T1 reusable pattern / T2 citation drift / T3 promotion / T4 style deviation / T5 standing rule / T6 capability gap) firing throughout work.
- Checkpoint 3: Four-way decision menu (capture-now / handle-now / backlog / drop) — every surfaced item routes through this exact shape.
- Checkpoint 4: Hard gates — 4.1 Compile / 4.2 Layered review / 4.3 Send / 4.4 State transition. Cannot be bypassed.
- Checkpoint 5: Source-grounding guard — every legal citation MUST be backed by a tool result (search_corpus / read_corpus_file). Training memory explicitly NOT a source.
- Checkpoint 6: Hard guards — German artifacts / memory write scopes / module-decision logging / baustein promotion guard.
- Checkpoint 7: Validation — citation freshness / style-spec diff / section-level edits / required sections.
- Checkpoint 8: Lifecycle (`draft → internal-review → sent-to-authority → awaiting-response → revision-requested → finalized → archived`) + ownership modes (migrate / new-work-only / quarantine).
- Checkpoint 9: MCP tool surface (NOT BUILT) — fallbacks documented for every tool.
- Checkpoint 10: Specialist routing table.
- Checkpoint 11: Project binding — first-bind flow for existing projects.
- Checkpoint 12: New-project creation — copies template tree from `ProjektBeispiel-B-Plan/` + instantiates `Projektdaten.tex`.
- Checkpoint 13: Conversational style — match user language, terse, commit to position.
- Checkpoint 14: Session close — recap in 4 lines max.

**Embedded PBS-specific discipline** (the exact thing pivot doc §3 wants to test on Cowork):
- **Sparring discipline**: NOT explicitly named "sparring" in the plugin, but adjacent shape exists in (a) review-draft's three-layer pause-between-layers + per-finding decision menu, (b) orchestrator's "commit to position; do not present open-ended choices," (c) source-grounding guard refusing to invent citations. These are sparring-shaped behaviors at lower abstraction than the framework's named sparring sub-mechanisms.
- **Defensibility**: orchestrator Checkpoint 6.3 (module-decision logging — "six months later when the UNB asks why a section is missing, the audit trail must exist"), Checkpoint 6.4 (baustein promotion guard requires source-project finalized), Checkpoint 8 (lifecycle state transitions require explicit acknowledgment). These directly serve "claim attribution chain across roles for 6-month-later defense."
- **Engagement detection**: orchestrator Checkpoint 1 distinguishes office / project / product scope from observable signals; this is engagement-detection-shaped scoping.

**Classification**:

- **TRANSPLANTABLE to Cowork SKILL.md with minor edits** (8 skills): `validate-checklist`, `validate-latex-style`, `verify-citations`, `validate-bausteine`, `save-baustein`, `promote-to-skill`, `record-feedback`, `draft-cover-mail`. These are mechanical/structural skills with clear procedures that don't depend heavily on the framework's MCP backend or runtime mechanisms. Adapt MCP-tool-call references to Cowork-MCP equivalents.
- **NEED SUBSTANTIVE REWRITE** (4 skills): `orchestrator` (Cowork's agent-routing primitives differ from this skill's auto-load + checkpoint-driven shape), `draft-textteil-b` + `draft-textteil-c` (hard-coded paths to `~/dev/Planungsbüro-Schulz/22-16-Maxsolar---Friedrichshof---B-Plan---Begruendung/` template; needs re-anchoring + corpus-search wiring on real MCP), `survey-project` (depends on file-clustering MCP that doesn't exist).
- **EMBODY PBS-SPECIFIC DISCIPLINE the pivot wants to test** (2 skills): `review-draft` (layered review + per-layer pause-and-decide is the exact sparring-shape pivot doc §3 names) + the `orchestrator` checkpoint discipline (gates + watch-list + source-grounding guard). These are the litmus-test items for "can custom SKILL.md files reach PBS-discipline depth on Cowork." `research-references` is adjacent — it embodies citation-currency discipline central to defensibility.

---

### C. Planungsbüro Schulz workspace

Path: `/mnt/data2t/hidrive/Öffentlich Planungsbüro Schulz/Projekte/`. **Treated as confidential — directory listing only; no project content reads.**

**Project count**: 18 active client projects (per directory naming pattern). Plus 4 special directories (Allgemein, Anfragen, Hendrik, Literatur, Projektleitung, Vorlagen, RREP Rostock + meta-state dirs).

**Naming pattern**: `YY-NN Client - Location` (e.g., `22-16 Maxsolar - Friedrichshof`, `25-06 PV Georgendorf`, `23-01 Ulrike Leye - Vogelsang`). Per orchestrator Checkpoint 12, this is the canonical office naming convention. Variant: `YY-NN-N Client - Location` (e.g., `22-24-2 W&R WPower - Wendisch Rambow`) for sub-projects.

Top recurring clients: Maxsolar (7 projects), W&R WPower (3), Solarfaktor, W&W. Strong PV/solar focus.

**Two template projects**: `ProjektBeispiel-Bauantrag` (Bauantrag template) + `ProjektBeispiel-B-Plan` (B-Plan template). Per orchestrator Checkpoint 12, new projects copy the canonical template tree from `ProjektBeispiel-B-Plan/`.

**Shared resources at root**:

`_ai-office-state/`:
- `projects-index.md` — YAML frontmatter + markdown body. Currently has only one entry (test entry from 2026-04-29: `24-99-test-foo`). Comment: "Maintained by pbs-mcp. Do not edit manually." Indicates **the projects-index sync mechanism does not yet run against the actual 18 projects** — only the test entry exists. This is a gap: the MCP backend that's supposed to populate it isn't built.
- `pending-actions.md`, `recent-correspondence.md` — office-level state files (not read).
- `templates/` — `office-identity.tex`, `office-logo.png`, `office-signature.png`, `office-style.sty`. Boilerplate for LaTeX deliverables.
- `extensions/MV/` — Mecklenburg-Vorpommern regional extensions (not read).

`_ai-references/`:
- `gesetze/{bund,eu,MV}/` — 3 jurisdiction subdirs **mostly empty** (only directory shells exist).
- `leitfaeden/` — empty.
- `urteile/` — empty.
- `beispiele/` — empty.
- `changelog.md` — contains literally "(empty)".

**MCP candidates surfaced by this workspace**:

1. **project-id-lookup MCP** — resolve `YY-NN` → full path; list active projects; bind by ID. The `projects-index.md` file is the obvious backing data; needs an MCP that maintains it from filesystem state.
2. **Baustein-corpus MCP** — `list_bausteine(scope, domain?)`, `get_baustein(name)`, `save_baustein(scope, name, content, source)`. Memory currently nominally lives in `<repo>/memory/global|domain|projects/<project>/...` per orchestrator routing rules; corpus root location is ambiguous (the `<repo>` reference doesn't resolve to the framework source repo). Needs concrete path decision in Cowork build.
3. **References-corpus MCP** — `search_corpus(query, k, filter)` over `_ai-references/{gesetze,leitfaeden,urteile,beispiele}` content. **Backing content is currently empty** — needs content-population step before MCP is useful.
4. **Per-project ingestion MCP** — `ingest_project_inputs(project, paths)` + `search_inputs(query, project, k)` for project-namespaced semantic search. Per skill descriptions, intended LanceDB + fastembed (BAAI/bge-m3); none of this runs today.
5. **LaTeX compile MCP** — `compile_latex(project_path)` running latexmk; currently fallback is direct `Bash latexmk -pdf` per skill descriptions.
6. **Project-scaffold MCP** — `scaffold_project(name, doctype, template)` from canonical template tree.
7. **File-clustering / survey MCP** — backing for `survey-project` skill: walk root, cluster files (artifacts / inputs / sent / correspondence / cruft).

Also: `~/dev/Planungsbüro-Schulz/` (4 canonical LaTeX template repos) is the office's **source-of-truth for LaTeX templates** per orchestrator's system map; the `Vorlagen/Latex/` folder on hidrive is explicitly NOT authoritative. Cowork build must reflect this asymmetry.

---

### D. Adjacent tooling

**Python-ACAD-Tools** at `/home/g/dev/Gunther-Schulz/Python-ACAD-Tools/`. Substantial codebase (`main.py` 11K LOC; src/ ~28+ modules: project_loader, layer_processor, dxf_exporter, dxf_utils, dxf_to_shapefiles, etc.). DXF/AutoCAD geo-CAD processing. **Not referenced by the pbs orchestrator**; orchestrator Checkpoint 8.4 mentions Hendrik's GIS workflow at `<project-root>/scripts/` + `workflow.yaml` is read-only. Plausibly **this** is what runs in Hendrik's joint-project scripts. Out of Cowork build scope (separate workflow, separate practitioner).

**gis_utils** at `/home/g/dev/Gunther-Schulz/gis_utils/`. Has its own Claude Code plugin (gis-utils MCP + skills: dxf-lageplan-extraction, schutzgebiete-analyse, buffer-zones-workflow, etc.). Per `README.md`: "GIS/CAD utility library and project workflow runner." Has MCP tools (catalog / list_recipes / list_templates / check_recipe_layers / get_function_help). **Already-functional Cowork-adjacent skill set targeting GIS work for Hendrik's joint projects + planning-office GIS prep**. Out of Cowork build scope for the planning-office text-document skills, BUT relevant as a **reference example** of how GIS + planning-office work compose at deployment level (per orchestrator Checkpoint 8.4 joint-project mode).

Neither is a candidate for transplant or rewrite. Both are independent tools the planning-office occasionally consumes outputs from.

---

## Cowork build implications

**Already-working that the Cowork build must preserve / wrap / not break**:
- The 14 skills' **operational workflow shape** — every checkpoint, gate, four-way menu, source-grounding guard, layered review, lifecycle state model, ownership modes, project binding, new-project scaffolding. This is the user's daily muscle memory.
- The **hidrive workspace layout convention** — `YY-NN Client - Location` naming + `_ai/` per-project state + canonical template-copy from `ProjektBeispiel-B-Plan/`. Both `~/dev/Planungsbüro-Schulz/` (canonical LaTeX templates) + hidrive `Projekte/` (active client work).
- The **office-identity LaTeX assets** (`_ai-office-state/templates/`) — `office-style.sty`, `office-logo.png`, `office-signature.png`, `office-identity.tex` are office-brand load-bearing.
- The **memory routing discipline** — global / domain / projects-scope, per orchestrator Checkpoint 6.2.

**Missing that the Cowork build needs to add**:
1. **Functional MCP backend** — every `search_corpus / read_corpus_file / list_bausteine / get_baustein / save_baustein / ingest_project_inputs / search_inputs / compile_latex / scaffold_project / list_projects / bind_project / survey_project` call. LanceDB + fastembed (`BAAI/bge-m3`) per plugin description, OR a Cowork-native equivalent. Whether to keep LanceDB or move to Cowork's preferred vector store is a design decision.
2. **References-corpus content seeding** — `_ai-references/{gesetze,leitfaeden,urteile,beispiele}/` directories exist but are mostly empty. Needs initial content population (BauGB / BNatSchG / DE-BB regional / leitfäden / urteile) before `search_corpus(filter={source_type: reference})` returns anything useful. The `research-references` skill is supposed to maintain freshness but has no baseline to maintain.
3. **Projects-index sync** — `_ai-office-state/projects-index.md` currently has 1 test entry vs 18 actual projects. The MCP that's supposed to maintain this from filesystem state needs to exist.
4. **Cross-session workspace state** — `pbs-dep-1` was the framework approach; with framework paused, equivalent persistence on Cowork primitives (Cowork agent state? per-project `_ai/state.md` only? Cowork event log?) is undefined.
5. **Per-project bausteine corpus** — orchestrator routes `save-baustein` to `<repo>/memory/projects/<project>/...` but the `<repo>` reference is ambiguous; the projects-scope memory location needs concrete decision in Cowork.

**In production but ripe for Cowork-native rewrite**:
1. **Orchestrator session-open auto-routing** — current pattern is "skill auto-loads on PBS-context detection from path/keywords." Cowork has its own agent-routing primitives (Managed Agents / Cowork agent definitions) that probably handle this differently. Likely a substantive rewrite, not a transplant.
2. **Project binding (survey-project)** — depends on a `survey_project` MCP that doesn't exist; rewriting this around Cowork-native file-clustering primitives or a custom MCP is a clean greenfield decision.
3. **Watch-list trigger surfacing (T1–T6)** — currently lives in PROCEDURE.md prose; could become explicit Cowork hooks/events with tighter semantics OR stay as SKILL.md prose. Pivot doc §3 names "engagement detection (rigor-on-demand)" as a Cowork test item — this is exactly that.
4. **Layered review pause-and-decide between layers** — currently SKILL.md prose. Pivot doc §3 names sparring discipline as Cowork test item — this is the most direct sparring-shaped behavior in the deployment. Worth examining whether Cowork HITL approval flows give it natively or whether SKILL.md prose remains the right container.

**MCP candidates surfaced** (consolidated):
- project-id-lookup / projects-index-sync
- baustein-corpus (list / get / save with scope routing)
- references-corpus (search / read with source_type filter)
- per-project-ingestion (LanceDB-style or Cowork-native vector store)
- LaTeX-compile (latexmk wrapper)
- project-scaffold (template-copy + Projektdaten.tex instantiation)
- file-clustering / survey
- correspondence-log (recent-correspondence.md sync from Schriftverkehr/*.eml)

---

## Honest basis caveats

**Files read directly in this session**:
- `/home/g/dev/Gunther-Schulz/pbs-dep-1/README.md`, `pyproject.toml`, `runtime/hello_pbs.py`, `findings/2026-05-06-m1-api-friction.md`, `workspace/workspace.md` (full reads).
- `/home/g/.claude/plugins/cache/pbs-bureau/pbs/0.1.0/.claude-plugin/plugin.json`, `CLAUDE.md` (full reads).
- `/home/g/.claude/plugins/cache/pbs-bureau/pbs/0.1.0/skills/orchestrator/SKILL.md`, `PROCEDURE.md` (full reads — both 96 + 425 LOC).
- `/home/g/.claude/plugins/cache/pbs-bureau/pbs/0.1.0/skills/draft-textteil-b/SKILL.md` (full read).
- `/home/g/.claude/plugins/cache/pbs-bureau/pbs/0.1.0/skills/review-draft/SKILL.md` (full read).
- Heads (first 10 lines) of remaining 11 SKILL.md files: `save-baustein`, `record-feedback`, `survey-project`, `research-references`, `promote-to-skill`, `verify-citations`, `validate-checklist`, `validate-bausteine`, `validate-latex-style`, `draft-cover-mail`, `draft-textteil-c` — descriptions read; full procedures NOT read.
- `/mnt/data2t/hidrive/Öffentlich Planungsbüro Schulz/Projekte/_ai-office-state/projects-index.md`, `_ai-references/changelog.md` (full reads of meta-files).
- `/home/g/dev/Gunther-Schulz/Python-ACAD-Tools/main.py` (first 50 LOC), `/home/g/dev/Gunther-Schulz/gis_utils/README.md` (first 40 LOC).
- `/home/g/dev/Gunther-Schulz/pbs-bureau/5-PIVOT-DECISION.md` (full — for context only).

**Files only listed (not read)**:
- 11 of 14 SKILL.md procedure-bodies (skimmed via head only — descriptions are reliable; full procedures not verified).
- `orchestrator/references/state-format.md` (290 LOC, listed; not read).
- `draft-textteil-b/PROCEDURE.md`, `review-draft/PROCEDURE.md` (referenced from SKILL.md text but not opened).
- All `pbs-dep-1/workspace/practitioners/test-practitioner.md` + `specialists/hello-specialist/*` (listed only).
- All `_ai-references/{gesetze,leitfaeden,urteile,beispiele}/` content (directories listed; subdirectory content not enumerated beyond top level — gesetze had `bund/eu/MV` subdirs only; leitfaeden/urteile/beispiele empty at top level).
- `_ai-office-state/pending-actions.md`, `recent-correspondence.md`, `templates/*`, `extensions/MV/*` (listed only).
- All `~/dev/Planungsbüro-Schulz/<template-dirs>/` content (only top-level dir names listed).
- All hidrive `Projekte/<YY-NN ...>/` directories (top-level only; one project `22-16 Maxsolar - Friedrichshof` had its first ~20 entries listed for orientation — content NOT inspected).

**Confidentiality boundary**: `Projekte/<YY-NN ...>/` content treated confidential per task instructions. Project counts + naming pattern + presence of subdirectories observed via `ls` only; no `.tex`, `.pdf`, `.docx`, `.eml`, or other client artifacts read.

**Inferences vs verified**:
- "**The pbs:* plugin is the actually-used production deployment**" — inferred from (a) plugin name + version 0.1.0 in cache (b) skill `description` fields explicitly cite hidrive path + German planning terminology (c) orchestrator auto-load triggers reference real client names. NOT verified by observing user actually invoking these skills in a session log.
- "**The MCP backend is NOT YET BUILT**" — inferred firmly from (a) every skill SKILL.md says "Tools used (when MCP backend lands)" or equivalent (b) orchestrator PROCEDURE.md §9 documents fallbacks for every MCP tool (c) `_ai-office-state/projects-index.md` has only test entry from 2026-04-29 (d) `_ai-references/` corpus largely empty. Strong convergent evidence; but not verified by attempting to call the MCP and observing failure.
- "**Reference corpus largely empty**" — verified by directly listing `_ai-references/{gesetze,leitfaeden,urteile,beispiele}/`: gesetze has only `{bund,eu,MV}` subdirs (deeper content NOT enumerated); leitfaeden/urteile/beispiele empty at top. Possible content lives deeper that wasn't traversed; flagged as not-fully-verified.
- "**Top-3 things already-working** + **Top-3 gaps**" — synthesis, not direct citation. Each derived from specific evidence above but the ranking is judgment.
- "**TRANSPLANTABLE / NEEDS-REWRITE / EMBODIES-PBS-DISCIPLINE classification of 14 skills**" — read-only of 3 SKILL.md files in full; remaining 11 classified from descriptions only. Classifications are reasonable but sub-skill-procedure detail not verified for the 11 skim-only skills.
- "**Adjacent tooling out of Cowork build scope**" — inferred from absence of any reference to `Python-ACAD-Tools` or `gis_utils` in the 3 read SKILL.md files + orchestrator PROCEDURE.md + plugin CLAUDE.md. NOT exhaustively grep-verified across all 14 skills; could be referenced in skim-only skills.
- "**Sparring discipline NOT explicitly named in plugin**" — verified: searched orchestrator SKILL.md + PROCEDURE.md + CLAUDE.md for the word "sparring" and other shape-discipline terms. Word does not appear. The "sparring-shaped behaviors" identification is inference from procedural shape, not citation.

Net: **Position**: pbs:* plugin is the live, daily-used planning-office deployment with substantial workflow muscle memory; pbs-dep-1 is a paused framework proof-of-life. **Recommendation**: Cowork build's first decisions are (1) which 8 transplantable skills to port first, (2) MCP backend stack choice (LanceDB + fastembed vs Cowork-native), (3) whether sparring-shape stays as SKILL.md prose or migrates to Cowork HITL primitives — those are the items pivot doc §3 explicitly names as the empirical test.
